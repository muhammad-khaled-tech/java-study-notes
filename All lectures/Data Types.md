

# Lesson 2: Data Types (The Primitives)

**(الدرس 2: أنواع البيانات - الأنواع البدائية)**

#### 1. Java: A Strongly Typed Language

**(لغة الجافا: لغة قوية التنميط)** **(Slide 66)**

##### 📖 The Surface (السطح / الظاهر)

- **القاعدة (The Rule):** كل متغير (variable) لازم يكون له نوع (type) معلن. وكل تعبير برمجي (expression) ليه نوع.
- **الفحص (The Check):** المترجم (Compiler) بيفحص **كل** عمليات التعيين (assignments) عشان يتأكد من توافق الأنواع.
- **النتيجة (The Result):** لو حاولت تحشر "شكل هندسي" (Shape) جوه متغير معمول عشان يشيل "رقم" (Number)، المترجم هيوقفك _قبل_ ما تقدر حتى تشغل البرنامج.

##### 👨‍🏫 Senior Touch / Pro Tips (لمسة السينيور / نصائح المحترفين)

> [!NOTE] **ليه "الصرامة" دي كويسة؟** في لغات زي JavaScript أو Python (اللي بنسميها loosely typed)، ممكن بالغلط تتعامل مع النص "10" (String) كأنه الرقم 10 (Number)، وده ممكن يسبب كراش للبرنامج والمستخدم شغال عليه بعد ساعات. في الجافا، الـ Compiler بيتصرف زي "بودي جارد" رخم واقف على باب النادي. بيقولك: _"أنت أعلنت المتغير ده إنه Integer. وأنت بتحاول تحط فيه تكست. مش هتدخل."_ ده بيمسك الـ Bugs بدري!

---

#### 2. The 8 Primitive Types

**(الأنواع البدائية الثمانية)** **(Slides 67 - 68)**

الجافا بتعرف **8** أنواع بدائية (primitive types) متقسمين لـ **4 مجموعات**:

1. **الأعداد الصحيحة (Integers):** `byte`, `short`, `int`, `long`.
2. **الأعداد العشرية (Floating-point):** `float`, `double`.
3. **الحروف (Characters):** `char`.
4. **القيم المنطقية (Boolean):** `boolean`.

##### 🧠 The Deep Dive: Why not Objects? (الغوص العميق: ليه مش كائنات؟)

Slide 68 بتذكر: _"تحويل الأنواع البدائية لكائنات (Objects) كان هيقلل الأداء بشكل كبير جداً."_

- **الكائنات (Heap):** الكائن (زي `String` أو `Integer`) عنده "حمل زيادة" (overhead) زي الـ metadata والـ headers. هو بيعيش في ذاكرة الـ **Heap** وبيحتاج تنظيف زبالة (Garbage Collection). تقيل زي الفيل. 🐘
- **البدائيات (Stack):** النوع البدائي (زي `int`) هو مجرد bits خام (أصفار وحايد). بيعيش في ذاكرة الـ **Stack** (غالباً). هو سريع جداً وكفء. خفيف زي الريشة. 🪶

##### 💡 Analogy: Portability (تشبيه: قابلية النقل)

- **في C/C++:** حجم الـ `int` بيتغير حسب الكمبيوتر (16-bit على الأجهزة القديمة، 32-bit على الجديدة). ده بيسبب bugs لما تنقل الكود بين كمبيوترات مختلفة.
- **في Java:** الـ `int` هو **دايماً 32-bits**، سواء شغلته على جهاز جيمنج ويندوز، أو ماك بوك، أو حتى ميكروويف. ده بيضمن **قابلية النقل (Portability)**.

---

#### 3. Group 1: The Integers

**(المجموعة 1: الأعداد الصحيحة)** **(Slide 69)**

الأنواع دي بتشيل أرقام صحيحة ليها إشارة (موجب وسالب).

|النوع (Type)|الحجم (Size)|المدى التقريبي (Range)|استخدام السينيور (Senior Advice)|
|:--|:--|:--|:--|
|**`byte`**|8 bits|-128 إلى 127|معالجة البيانات الخام (صور، ملفات).|
|**`short`**|16 bits|-32,768 إلى 32,767|نادراً ما يستخدم النهاردة. ممكن في المصفوفات المحسنة (optimized arrays).|
|**`int`**|32 bits|-2 مليار إلى +2 مليار|**الاختيار الافتراضي.** استخدمه لعدادات اللوب (loop counters) والحسابات.|
|**`long`**|64 bits|ضخم جداً (كوينتيليون)|استخدمه للوقت (مللي ثانية)، الـ IDs، أو الحسابات الضخمة.|

> [!TIP] **فخ الـ `long`** لو كتبت `long number = 9999999999;` ممكن يديك Error. ليه؟ المترجم بيفتكر إن الرقم 9999... ده `int` وبيشتكي إنه كبير أوي. لازم تضيف حرف 'L' في الآخر: `long number = 9999999999L;`.

---

#### 4. Group 2: Floating-Point Numbers

**(المجموعة 2: الأعداد العشرية)** **(Slide 70)**

دي بتمثل الأرقام اللي فيها كسور عشرية. الجافا بتتبع معيار **IEEE-754**.

|النوع (Type)|الحجم (Size)|المدى (Range)|الدقة (Precision)|
|:--|:--|:--|:--|
|**`float`**|32 bits|صغير|دقة أحادية (سريع بس أقل دقة).|
|**`double`**|64 bits|كبير|دقة مزدوجة (ده الافتراضي والقياسي).|

##### 🧠 The Deep Dive: The Precision Problem (الغوص العميق: مشكلة الدقة)

الأرقام العشرية (Floating-point) هي عبارة عن **تقديرات (approximations)**.

- في الرياضيات: 0.1 + 0.2 = 0.3.
- في الجافا (ومعظم الكمبيوترات): 0.1 + 0.2 ممكن تطلع `0.30000000000000004`.

> [!WARNING] **خطأ قاتل (Fatal Mistake)** **أوعى أبداً** تستخدم `float` أو `double` لحسابات الفلوس/المالية. الأخطاء الصغيرة دي بتتراكم. **حل السينيور:** استخدم كلاس `BigDecimal` للفلوس.

---

#### 5. Group 3: Characters

**(المجموعة 3: الحروف)** **(Slide 71)**

##### 📖 The Surface (السطح)

- **النوع:** `char`.
- **الحجم:** 16-bit (2 بايت).
- **المعيار:** بيستخدم **Unicode** (تحديداً UTF-16).

##### 💡 Analogy: ASCII vs. Unicode (تشبيه: ASCII مقابل Unicode)

- **ASCII (الطريقة القديمة):** بتستخدم 8 bits. بتقدر تمثل بس الحروف الإنجليزية (A...Z) والرموز الأساسية. ده كان كفاية لأمريكا في التمانينات.
- **Unicode (طريقة جافا):** بيستخدم 16 bits. بيقدر يمثل الإنجليزي، **العربي**، الصيني، الياباني، والإيموجيز. الجافا اتصممت عشان الويب "العالمي" (World Wide Web)، فكانت محتاجة تتكلم كل لغة.

---

#### 6. Group 4: Booleans

**(المجموعة 4: القيم المنطقية)** **(Slide 72-73)**

##### 📖 The Surface (السطح)

- **النوع:** `boolean`.
- **القيم:** ممكن يكون **فقط** `true` أو `false`.
- **الاستخدام:** بيستخدم في التعبيرات الشرطية (`if`, `while`, `for`).

##### 🧠 The Deep Dive: C++ vs. Java (الغوص العميق: C++ مقابل Java)

ده سؤال إنترفيو كلاسيكي.

- **في C++:** الأعداد الصحيحة ممكن تكون booleans. الصفر يعني false، والواحد (أو أي رقم غير صفري) يعني true.
    - `if (1) { ... }` -> شغال.
- **في Java:** الأعداد الصحيحة **ليست** booleans.
    - `if (1) { ... }` -> **خطأ مترجم** (أنواع غير متوافقة: لا يمكن تحويل int لـ boolean).
    - لازم تكتب: `if (x != 0) { ... }`.

---

### Lesson 2 (Part 3): Literals and Variables

**(الدرس 2 - الجزء 3: القيم الثابتة والمتغيرات)**

#### 1. Integer Literals: The Formats

**(القيم الثابتة الصحيحة: الصيغ)** **(Slide 74-75)**

##### 📖 The Surface (السطح)

تقدر تكتب رقم صحيح في الجافا بـ 4 طرق مختلفة:

1. **عشري (Decimal - Base 10):** الأرقام العادية (10, 100).
2. **ثماني (Octal - Base 8):** يبدأ بـ 0 (مثلاً `012` هي 10 بالعشري).
3. **سداسي عشر (Hexadecimal - Base 16):** يبدأ بـ 0x أو 0X (مثلاً `0xFF`).
4. **ثنائي (Binary - Base 2):** يبدأ بـ 0b أو 0B (تم إضافته في JDK 7).

##### 🧠 The Deep Dive: Underscores (الغوص العميق: الشرطات السفلية)

بداية من **JDK 7**، تقدر تحط شرطات سفلية (`_`) جوه الأرقام عشان تخليها مقروءة. المترجم بيتجاهلها تماماً.

- **الإنسان:** 1000000 (ده مليون ولا 100 ألف؟)
- **محترف جافا:** `1_000_000` (واضح إنه مليون).

> [!WARNING] **فخ الصفر البادئ (The Leading Zero Trap)** أوعى تحط صفر على الشمال عشان تخلي الأرقام "شكلها محاذي" في الكود (مثلاً `09`). الجافا هتفتكر إنك بتكتب بـ **Octal** (نظام ثماني)، وبما إن الرقم 9 مش موجود في النظام الثماني، هيطلعلك Compiler Error!

---

#### 2. Floating-Point Literals

**(القيم الثابتة العشرية)** **(Slides 75 - 77)**

##### 📖 The Surface (السطح)

- **الصيغة القياسية:** `3.14159`
- **الصيغة العلمية:** `6.02E23` (يعني 6.02 × 10²³)
- **Hexadecimal Floating-Point:** `0x12.2P2` (بيستخدم P لقوى العدد 2، نادراً ما يستخدم بس دقيق).

##### 👨‍🏫 Senior Touch / Pro Tips (لمسة السينيور)

> [!TIP] **لاحقة الـ F (The F Suffix)** افتراضياً، أي رقم عشري في الجافا (3.14) بيتعامل كأنه **double** (64-bit). لو حاولت تحشره جوه `float` (32-bit)، المترجم هيطلع Error.
> 
> - **غلط:** `float f = 3.14;` (Error: احتمال فقدان دقة).
> - **صح:** `float f = 3.14F;` (اللاحقة F بتقول للمترجم "عامل ده كأنه float 32-bit").

---

#### 3. Boolean & Character Literals

**(القيم الثابتة المنطقية والحرفية)** **(Slides 77 - 79)**

##### Boolean Literals

- قيمتين صحيحتين فقط: `true` و `false`.
- **هام:** هما _لا_ يساووا 1 أو 0.

##### Character Literals

- محاطة بـ **علامات تنصيص مفردة** `' '`.
- ممكن تكون حرف (`'a'`) أو **تتابع هروب (Escape Sequence)**.
- **دعم Unicode:** تقدر تعرف الـ char باستخدام الكود السداسي بتاعه: `'\u0061'` (اللي هو `'a'`).

|التتابع|المعنى|
|:--|:--|
|`\n`|سطر جديد (Enter)|
|`\t`|مسافة (Tab)|
|`\'`|علامة تنصيص مفردة|
|`\\`|شرطة مائلة خلفية (Backslash)|

---

#### 4. String Literals

**(القيم الثابتة النصية)** **(Slide 80)**

##### 📖 The Surface (السطح)

- محاطة بـ **علامات تنصيص مزدوجة** `" "`.
- مثال: `"Hello World"`.
- Slide 80 بتنص: _"النصوص (Strings) هي فعلياً أنواع كائنات (object types)."_

##### 🧠 The Deep Dive: The String Pool (الغوص العميق: مسبح النصوص)

على عكس `int` أو `char`، الـ `String` هو كائن معقد (Complex Object). ومع ذلك، الجافا بتسمحلك تنشئهم باستخدام القيم الثابتة (`"Text"`) بدل ما تكتب `new String("Text")`.

- **ليه؟** عشان التحسين (Optimization).
- **الـ Pool:** لما بتعمل String literal، الجافا بتفحص منطقة خاصة في الذاكرة اسمها **String Pool**. لو "Hello" موجودة هناك بالفعل، بتعيد استخدامها بدل ما تخلق كائن جديد. ده بيوفر كميات مهولة من الذاكرة.

---

### Lesson 2 (Part 4): Variables, Scope, & Type Promotion

**(الدرس 2 - الجزء 4: المتغيرات، النطاق، وترقية الأنواع)**

#### 1. Variable Declaration & Dynamic Initialization

**(إعلان المتغيرات والتهيئة الديناميكية)** **(Slides 82 - 83)**

##### 📖 The Surface (السطح)

- **الإعلان:** `type identifier [= value];`
- **التهيئة الديناميكية:** مش لازم تعين أرقام ثابتة (hardcoded). تقدر تعين _نتيجة عملية حسابية_ للمتغير في لحظة إعلانه.

##### 👨‍🏫 Senior Touch / Pro Tips (لمسة السينيور)

> [!TIP] **قاعدة الكود النضيف (Clean Code Rule)** في اللغات القديمة (زي C89)، كنت مضطر تعلن عن كل المتغيرات في أول الدالة. **في الجافا:** أعلن عن المتغير **بالظبط في المكان اللي محتاجه فيه**، مش قبله. ده بيخلي الكود مقروء وبيمنعك من استخدام متغير غير مهيأ (uninitialized) بالغلط.

---

#### 2. Scope and Lifetime (The Stack)

**(النطاق والعمر الافتراضي - المكدس)** **(Slides 84 - 85)**

##### 📖 The Surface (السطح)

- **نطاق الكتلة (Block Scope):** الكتلة بتتعرف بالأقواس المعقوفة `{ }`.
- **القاعدة:** المتغيرات المعلنة جوه كتلة بتكون **غير مرئية** خارجها.
- **العمر الافتراضي:** لما الكتلة تنتهي `}`, المتغير بيتدمر.

##### 🧠 The Deep Dive (Stack Memory) / (الغوص العميق - ذاكرة المكدس)

كل مرة بتفتح قوس معقوف `{`، أنت بتعمل push لـ **Stack Frame** جديد (أو نطاق منطقي جوه الـ Frame) في الميموري.

1. **دخول `{`:** الجافا بتحجز ميموري للمتغيرات المعلنة هنا.
2. **خروج `}`:** الجافا بتعمل "pop" للميموري دي. بتختفي للأبد. عشان كدة مش بتقدر توصل لمتغير متعرف جوه `if` بعد ما الـ `if` تقفل.

##### 💡 Analogy: One-Way Glass (تشبيه: الزجاج ذو الاتجاه الواحد)

فكر في النطاقات (scopes) زي الزجاج الفيميه في العربية.

- **النطاق الداخلي (جوه العربية):** تقدر تشوف كل حاجة بره (المتغيرات الخارجية).
- **النطاق الخارجي (بره العربية):** مش بتقدر تشوف إيه اللي جوه (المتغيرات الداخلية).
```java

//Demonstrate block scope.
class Scope {
public static void main(String args[]) {
int x; // known to all code within main
x = 10;
if(x == 10) { // start new scope
int y = 20; // known only to this block
// x and y both known here.
System.out.println("x and y: " + x + " " + y);
x = y * 2;
}
// y = 100; // Error! y not known here // x is still known here.
System.out.println("x is " + x);
}
}
```

---

#### 3. Type Conversion (Widening)

**(تحويل الأنواع - التوسيع)** **(Slides 86 - 87)**

##### 📖 The Surface (السطح)

أحياناً بتعين نوع لنوع تاني. الجافا بتعمل ده **أوتوماتيكياً** لو توفر شرطين:

1. الأنواع تكون **متوافقة (compatible)**.
2. النوع الهدف يكون **أكبر** من النوع المصدر. دي بنسميها **Widening Conversion**.

##### 🧠 The Deep Dive (الغوص العميق)

- **متوافقة:** الأرقام متوافقة مع الأرقام. الـ `char` بيتصرف كرقم. الـ `boolean` غير متوافق مع أي حاجة.
- **ليه آمنة؟**
    - تخيل `byte` (8 bits): `00000101` (القيمة 5).
    - عينه لـ `int` (32 bits): `00000000 00000000 00000000 00000101` (لسه 5).
    - الجافا بس زودت أصفار (حشو - padding). مفيش داتا ضاعت.

##### 📉 مسار التحويل (Conversion Flow)

`byte` -> `short` -> `int` -> `long` -> `float` -> `double`

---

#### 4. Casting (Narrowing)

**(التحويل القسري - التضييق)** **(Slide 88)**

##### 📖 The Surface (السطح)

ماذا لو عايز تحط `int` جوه `byte`؟ الجافا هترفض لأن الداتا ممكن تضيع.

- **الحل:** لازم تعمل **Cast** بشكل صريح.
- **الصيغة:** `(target-type) value;`

##### 🧠 The Deep Dive (Bit Truncation) / (الغوص العميق - قطع البتات)

دي عملية خطيرة. أنت بتقول للمترجم: _"أنا عارف إن ده ممكن يفشل، بس ثق فيا."_ لو الرقم كبير جداً على الهدف، الجافا بتعمل **truncate** (بتقص) البتات العالية. **مثال:** عمل Casting لـ int 257 لـ byte.

- int 257: `... 00000001 00000001`
- byte cast: (بيرمي اللي فوق) -> `00000001`
- **النتيجة:** 1 (ضياع ضخم للبيانات!).

---

#### 5. Automatic Type Promotion in Expressions

**(الترقية التلقائية للأنواع في التعبيرات)** **(Slide 89)**

##### 📖 The Surface (السطح)

دي قاعدة محددة جداً في معادلات الجافا.

- **القاعدة:** لما بتحسب تعبير (زي `a * b`)، الجافا أوتوماتيكياً بترقي معاملات `byte` و `short` و `char` لـ **`int`**.

##### 🧠 The Deep Dive (Why??) / (الغوص العميق - ليه؟؟)

البروسيسورات (CPUs) محسنة لعمليات 32-bit (أو 64-bit). عمل حسابات على bytes بحجم 8-bit هو فعلياً أصعب/أبطأ للـ CPU. عشان كدة الجافا بتحول كل حاجة لـ int الأول، تعمل الحسبة، وتديك ناتج int.

##### 💻 Code Autopsy: The "Byte Trap" (فخ البايت)

Slide 89 بتقدم مشكلة كلاسيكية:

> [!WARNING] **خطأ الترجمة (Compilation Error)** لو جربت ده: `byte d = a * b / c;` **Compiler Error:** "أنواع غير متوافقة: احتمال فقدان دقة من int لـ byte". **الحل:** لازم تعمل cast لـ _النتيجة_ وترجعها byte تاني: `byte d = (byte) (a * b / c);`

---

### Lesson 2 (Part 5): Arrays

**(الدرس 2 - الجزء 5: المصفوفات)**

> [!ABSTRACT] **نظرة عامة على المحاضرة** بنتحرك من المتغيرات الفردية لـ **مجموعات (Collections)** من البيانات. هنغطي المصفوفات أحادية البعد، إزاي الذاكرة بتتحجز ليها، المصفوفات متعددة الأبعاد (Matrices)، وأخيراً هنحل معمل مقارنة الأداء (benchmark lab).

---

#### 1. One-Dimensional Arrays

**(المصفوفات أحادية البعد)** **(Slides 91 - 93)**

##### 📖 The Surface (السطح)

- **التعريف:** المصفوفة هي مجموعة من المتغيرات **من نفس النوع** بيشار إليها باسم مشترك.
- **الإعلان:** `type var_name[];` (مثلاً `int month_days[];`).
- **الحجز (Allocation):** **لازم** تستخدم المعامل `new` عشان تحجز ميموري. `array-var = new type [size];`.

##### 🧠 The Deep Dive (Memory & Objects) / (الغوص العميق - الذاكرة والكائنات)

ده مفهوم حاسم للمطور السينيور.

- **Arrays are Objects:** في C++، المصفوفة مجرد مؤشر (pointer) لكتلة ذاكرة. في Java، المصفوفة هي **Object** كامل. بتعيش في الـ **Heap**.
- **المعامل `new`:** لما بتكتب `int arr[]`، الـ `arr` ده مجرد مرجع (قيمته حالياً null). لما بتكتب `new int`، الـ JVM بتلاقي كتلة متصلة من الذاكرة لـ 12 رقم صحيح في الـ Heap، بتصفرهم (تخليهم أصفار)، وبتسلم العنوان لـ `arr`.
- **القيم الافتراضية:** عكس الـ C، الجافا بتهيئ المصفوفات أوتوماتيكياً.
    - `int[]` -> تتملي 0.
    - `boolean[]` -> تتملي false.
    - `Object[]` -> تتملي null.

##### 👨‍🏫 Senior Touch / Pro Tips (لمسة السينيور)

> [!TIP] **أسلوب كتابة الكود (Syntax Style)** الجافا بتدعم أسلوبين:
> 
> 1. `int month_days[]` (أسلوب C).
> 2. `int[] month_days` (أسلوب Java). **نصيحة السينيور:** دايماً استخدم الأسلوب رقم 2 (`int[] name`). دي بتتقري بوضوح "المتغير ده عبارة عن مصفوفة أعداد صحيحة". الأسلوب رقم 1 اتضاف بس عشان يساعد مبرمجي C ينقلوا للجافا في التسعينات.

---

#### 2. Multi-Dimensional Arrays

**(المصفوفات متعددة الأبعاد)** **(Slides 94 - 95)**

##### 📖 The Surface (السطح)

- **التعريف:** الجافا بتنفذ دول كـ "مصفوفات من مصفوفات" (Arrays of Arrays).
- **النحو:** `int twoD[][] = new int;`
- **بصرياً:** فكر فيها كمصفوفة (Matrix) فيها صفوف (المؤشر الأيسر) وأعمدة (المؤشر الأيمن).

##### 🧠 The Deep Dive (The Jagged Array) / (الغوص العميق - المصفوفة المسننة)

بما إن الجافا بتتعامل مع المصفوفات متعددة الأبعاد كـ "مصفوفة من كائنات (حيث الكائنات دي هي مصفوفات تانية)"، الصفوف مش لازم تكون نفس الطول!

- **Regular Matrix:** `new int` (4 صفوف، كلهم طولهم 5).
- **Jagged Array:** الصف الأول طوله 3، التاني طوله 10.
- ده مفيد لتحسين الذاكرة لو هيكل البيانات بتاعك مثلثي (زي مثلث باسكال).

---

#### 3. Lab Exercise: Performance & Algorithms

**(تمرين المعمل: الأداء والخوارزميات)** **(Slide 96)**

ده الشغل الحقيقي. السلايد بتطلب مننا نقارن بين **البحث الخطي (Linear Search)** و **البحث الثنائي (Binary Search)** ونقيس الوقت.

> [!NOTE] **المهمة (The Mission)**
> 
> 1. انشئ مصفوفة فيها 1000 عنصر.
> 2. أوجد الـ Min/Max واحسب وقت التشغيل.
> 3. أعد الكتابة باستخدام **Binary Search** واحسب وقت التشغيل.
> 4. **تلميح:** استخدم `System.nanoTime()` للدقة.

##### 🧪 Solution 1: The Manual Approach (Linear Scan)

في الطريقة دي، بنزور كل عنصر واحد واحد (O(n)).

##### 🧪 Solution 2: The Sort & Binary Search Approach

عشان تستخدم Binary Search، المصفوفة **لازم تكون مترتبة (sorted)**. بمجرد ترتيبها، الـ "Min" بيكون حصرياً في الـ index 0، والـ "Max" في الـ index length-1.

##### 👨‍🏫 Senior Code Autopsy & Analysis (تحليل السينيور للكود)

1. **تعقيد الوقت (Time Complexity):**
    - **Linear Scan:** هو **O(n)**. بتلمس 1000 عنصر. سريع للمصفوفات الصغيرة، بطيء للملايين.
    - **Sorting approach:** هو **O(n log n)**. الترتيب مكلف!
    - **Binary Search (بعد الترتيب):** هو **O(log n)**. سريع جداً جداً.
2. **النتيجة:** غالباً هتلاقي إنه لـ 1000 عنصر بس، **الـ Linear Scan فعلياً أسرع** لأن `Arrays.sort()` ليها overhead. لكن، لو عندك 10 مليون عنصر ومحتاج تبحث 50 مرة، طريقة الـ Sorting/Binary تكسب.

> [!WARNING] **فخ السينيور: currentTimeMillis vs nanoTime** السلايد بتقترح استخدام `System.currentTimeMillis()`.
> 
> - **لا تستخدم** `currentTimeMillis` لقياس سرعة تنفيذ الكود. هي بتعتمد على ساعة نظام التشغيل وممكن تكون غير دقيقة بفرق 10-20 مللي ثانية.
> - **دايماً استخدم** `System.nanoTime()` لاختبارات الأداء (benchmarks). هي بتقيس دورات المعالج ودقيقة للنانوثانية.