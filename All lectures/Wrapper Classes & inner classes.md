
# Lesson 5 (Part 1): Wrapper Classes & Autoboxing

**(كلاسات التغليف والتعليب التلقائي)**

#### 1. The Bridge: Primitive to Object

**(الجسر: من بدائي لكائن)** **(Source:)**

##### 📖 The Surface (السطح)

في الجافا، عندنا عالمين منفصلين:

1. **Primitives:** أنواع بدائية بسيطة وسريعة (`int`, `double`, `boolean`).
2. **Objects:** كائنات معقدة بتعيش في الـ Heap (`String`, `Scanner`, `ArrayList`).

الـ **Wrapper Classes** هي الكلاسات اللي "بتغلف" النوع البدائي عشان تخليه يبان كأنه Object.

|Primitive Type|Wrapper Class (The Object)|
|:--|:--|
|`int`|**`Integer`** (الاسم بالكامل)|
|`char`|**`Character`** (الاسم بالكامل)|
|`byte`|`Byte`|
|`double`|`Double`|

##### 🧠 The Deep Dive: Stack vs. Heap (الغوص العميق)

إيه الفرق الحقيقي في الميموري؟ ده اللي يهمك كسينيور.

- **Primitive (`int x = 5;`):**
    - **المكان:** Stack مباشرة.
    - **الحجم:** 4 bytes صافي.
    - **الأداء:** صاروخ (Rocket fast). مجرد قيمة خام، مفيش overhead.
- **Wrapper (`Integer y = new Integer(5);`):**
    - **المكان:** Heap.
    - **الهيكل:** ده Object كامل. ليه Header (حوالي 12 bytes) + Metadata + القيمة نفسها.
    - **التكلفة:** تقيلة. الـ CPU محتاج يروح للـ Heap ويمشي ورا الـ References.

---

#### 2. Why use Wrappers?

**(ليه نستخدمهم أصلاً؟)** **(Source:)**

لو الـ Wrapper تقيل كده، ليه بنستخدمه؟ المصادر بتحدد 3 أسباب قاتلة:

1. **Object Requirements (متطلبات الكائنات):**
    - الـ `Collections` زي `ArrayList` أو `HashMap` **مبتقبلش** Primitives.
    - مينفعش تكتب `ArrayList<int>`. لازم تكتب `ArrayList<Integer>`.
2. **Constants (حدود الهاردوير):**
    - عايز تعرف أقصى رقم يتحمله الـ integer؟ الـ Wrapper هو اللي شايل المعلومة دي: `Integer.MAX_VALUE` (اللي هو $2^{31}-1$).
3. **Utility Methods (أدوات التحويل):**
    - إزاي تحول نص "123" لرقم؟ `int` ميعرفش يعمل كده. `Integer` يعرف: `Integer.parseInt("123")`.

---

#### 3. The Toolkit (Parsing and Viewing)

**(صندوق الأدوات: التحليل والعرض)** **(Source:)**

##### 💻 Code Autopsy (تشريح الكود)

دي الأكواد اللي بتستخدمها يومياً في الـ Production عشان تحول البيانات الجاية من الـ Frontend (String) لأرقام.

```java
public class WrapperUtils {
    public static void main(String[] args) {
        String strNum = "123";

        // 1. Parsing: String -> int (Primitive)
        // دي أكتر ميثود هتستخدمها في حياتك
        int num = Integer.parseInt(strNum);

        // 2. ValueOf: String -> Integer (Object)
        // بتستخدم الكاش (Cache) لو الرقم صغير
        Integer numObj = Integer.valueOf(strNum);

        // 3. Conversion: Decimal -> Binary/Hex
        // أدوات للمهندسين اللي بيتعاملوا مع Low-level
        String binary = Integer.toBinaryString(10); // "1010"
        String hex = Integer.toHexString(255);      // "ff"

        System.out.println("Parsed: " + num);
        System.out.println("Binary: " + binary);
    }
}
```

---

#### 4. Special Numbers (Infinity & NaN)

**(الأرقام الخاصة: اللانهاية وما ليس رقماً)** **(Source:,)**

##### 📖 The Surface (السطح)

في التعامل مع الأرقام الصحيحة (`int`)، القسمة على صفر `1/0` بتعمل كراش (`ArithmeticException`). لكن في الأرقام العشرية (`Double` / `Float`)، الجافا بتطبق قواعد الفيزياء والرياضيات الحقيقية.

- **`POSITIVE_INFINITY`**: ناتج `1.0 / 0.0`.
- **`NEGATIVE_INFINITY`**: ناتج `-1.0 / 0.0`.
- **`NaN` (Not a Number)**: ناتج `0.0 / 0.0` أو جذر تربيعي لرقم سالب.

##### 👨‍🏫 Senior Touch: The NaN Trap (لمسة السينيور: فخ الـ NaN)

> [!WARNING] **NaN is Toxic (الـ NaN فيروسي)** الـ `NaN` عامل زي الفيروس. لو دخل في أي معادلة، الناتج بيبقى `NaN`.
> 
> - `5 + NaN = NaN`
> - `NaN > 0` نتيجتها `false`
> - `NaN == NaN` نتيجتها `false` (دي أغرب حاجة!).
> 
> **الحل:** دايماً افحص القيمة بالميثود دي: `if (Double.isNaN(myValue)) { ... }`

---

### Lesson 5 (Part 2): Autoboxing & Unboxing

**(التعليب وفك التعليب التلقائي)** **(Source:,)**

#### 1. The Concept (The Illusion)

**(المفهوم - الوهم)** قبل Java 5، كان لازم تكتب كود طويل عشان تحول من `int` لـ `Integer`. دلوقتي الكومبايلر بيعمل ده بالنيابة عنك.

- **Autoboxing:** وضع الـ Primitive جوه Wrapper (تلقائياً).
- **Unboxing:** استخراج الـ Primitive من Wrapper (تلقائياً).

#### 2. Code Autopsy: What you write vs. What Java sees

**(تشريح الكود: اللي بتكتبه مقابل اللي بيحصل)** **(Source:)**

```java
// 1. Autoboxing
Integer x = 10;
// المترجم بيحولها لـ:
// Integer x = Integer.valueOf(10);

// 2. Unboxing
int y = x;
// المترجم بيحولها لـ:
// int y = x.intValue();
```

#### 3. 🧠 The Deep Dive: The Two Deadly Traps

**(الغوص العميق: الفخين القاتلين)** **(Source:)**

الأوتوبوكسينج مريح، بس بيخفي وراه كارثتين لو مش واخد بالك.

##### Trap 1: The Hidden Performance Cost (التكلفة الخفية)

تخيل الكود ده في Loop:

```java
Integer sum = 0; // لاحظ: Integer Object مش int primitive
for (int i = 0; i < 1000; i++) {
    sum = sum + i;
}
```

**ما وراء الكواليس (The Horror):** السطر `sum = sum + i` بيتحول لـ:

1. `sum.intValue()` (Unboxing عشان يجمع).
2. `+ i` (الجمع).
3. `Integer.valueOf(...)` (Autoboxing عشان يخزن النتيجة تاني). **النتيجة:** أنت خلقت **1000 Integer Object** في الميموري ورميتهم في الزبالة (Garbage) بس عشان تعمل عملية جمع بسيطة! **نصيحة سينيور:** دايماً استخدم `int` (primitives) في الحسابات والـ Loops.

##### Trap 2: The Null Pointer Exception (NPE)

بما إن الـ Wrapper هو Object، فهو ممكن يكون قيمته `null`.

```java
Integer x = null;
int y = x; // Boom! NullPointerException 💥
```

**السبب:** المترجم بيحاول ينادي `x.intValue()`. وبما إن `x` بـ null، الكود بيضرب.

---

# Lesson 5 (Part 3): Inner Classes (Types 1 & 2)

**(الكلاسات الداخلية - الجزء الأول)**

#### 1. Why Inner Classes? (The Philosophy)

**(ليه بنحتاج كلاسات داخلية؟ - الفلسفة)** **(Sources:,)**

##### 📖 The Surface (السطح)

الجافا بتسمحلك تعرف `class` جوه `class` تاني. دي مش فزلكة، دي ضرورة معمارية لـ 3 أسباب:

1. **Logical Grouping (التجميع المنطقي):**
    - لو عندك كلاس `CarEngine` مش بيستخدمه أي حد في المشروع غير كلاس `Car`. ليه تخليه `public` وتلوث الـ Global Namespace؟ خبيه جوه `Car`.
2. **Enhanced Encapsulation (كبسولة فائقة):**
    - دي النقطة الحاسمة. الـ Inner Class يقدر يشوف الـ **private members** بتوع الـ Outer Class. كأنه فرد من العيلة عايش معاك في نفس البيت.
3. **Readability (القراءة):**
    - الكود بيبقى قريب من المكان اللي بيستخدم فيه.

---

#### 2. Type 1: Normal Member Inner Class

**(النوع الأول: الكلاس الداخلي العضو)** **(Sources:,)**

##### 📖 The Surface (السطح)

ده كلاس بيتعرف في المستوى الرئيسي للكلاس الخارجي (زي الميثودز والمتغيرات).

- **القاعدة الذهبية:** "لا وجود للابن بدون الأب".
- عشان تعمل Object من الـ Inner، لازم يكون عندك Object عايش من الـ Outer.

##### 💻 Code Autopsy (تشريح الكود)

_(بناءً على Slides 236 & 239)_

```java
class Outer {
    private int x = 100; // متغير خاص

    // Member Inner Class
    class Inner {
        void display() {
            // السحر هنا: القدرة على الوصول للمتغير الخاص x مباشرة
            System.out.println("Value of x: " + x);
        }
    }

    // ميثود في الكلاس الخارجي بتستخدم الداخلي
    void createInner() {
        Inner in = new Inner();
        in.display();
    }
}
```

##### 👨‍🏫 Senior Touch: Instantiation Syntax

**(طريقة الإنشاء الغريبة)** لو عايز تعمل `Inner` من بره الكلاس (مثلاً في `main`)، الصيغة بتبقى غريبة شوية:

```java
public class Test {
    public static void main(String[] args) {
        // 1. لازم نكريت الأب الأول
        Outer outerObj = new Outer();

        // 2. نستخدم الأب عشان نكريت الابن
        // لاحظ الصيغة: reference.new
        Outer.Inner innerObj = outerObj.new Inner();

        innerObj.display();
    }
}
```

##### 🧠 The Deep Dive: The Hidden Reference

**(الغوص العميق: الرابط الخفي)** **(Source:)** لما تعمل Compile، الجافا بتطلع ملفين:

1. `Outer.class`
2. `Outer$Inner.class` (علامة الدولار بتفصل بينهم).

**إيه اللي بيحصل جوه الميموري؟** الكومبايلر بيغير الـ Constructor بتاع الـ `Inner` وبيبعتله "نسخة" من الـ `Outer` في السر. الكود المخفي بيبقى شكله كده تقريباً:

```java
class Outer$Inner {
    // حقل مخفي بيشاور على الأب
    final Outer this$0;

    // الكومبايلر بيزود المعامل ده غصب عنك
    Outer$Inner(Outer parent) {
        this.this$0 = parent;
    }

    void display() {
        // بيستخدم الرابط الخفي عشان يوصل لـ x
        System.out.println(this.this$0.x);
    }
}
```

> **خطر الذاكرة (Memory Leak):** بما إن الـ Inner ماسك في الـ Outer، لو الـ Inner فضل عايش، الـ Garbage Collector **مستحيل** يمسح الـ Outer، حتى لو أنت خلصت شغل بيه.

---

#### 3. Type 2: Static Member Inner Class

**(النوع الثاني: الكلاس الداخلي الثابت)** **(Sources:,)**

##### 📖 The Surface (السطح)

ده كلاس داخلي بس واخد keyword **`static`**.

- **القاعدة الذهبية:** "الابن المستقل".
- مش محتاج Object من الـ Outer عشان يعيش. هو مجرد كلاس عادي بس "ساكن" عند الـ Outer عشان التنظيم.

##### 💻 Code Autopsy (تشريح الكود)

_(بناءً على Slide 241)_

```java
class OuterStatic {
    private static int y = 200; // لازم يكون static عشان الـ Inner يشوفه

    // Static Inner Class
    static class InnerStatic {
        void show() {
            System.out.println("Static y: " + y);
            // System.out.println(x); // ❌ Error: مقدرش أشوف المتغيرات العادية (Non-static)
        }
    }
}

public class TestStatic {
    public static void main(String[] args) {
        // مش محتاجين نعمل new OuterStatic()
        // بنناديه باسم الكلاس مباشرة
        OuterStatic.InnerStatic obj = new OuterStatic.InnerStatic();
        obj.show();
    }
}
```

##### 🧠 The Deep Dive: Memory Usage (Light vs. Heavy)

**(مقارنة الذاكرة: الخفيف ضد الثقيل)** **(Source:)**

|وجه المقارنة|Normal Inner Class|Static Inner Class|
|:--|:--|:--|
|**الاعتمادية**|محتاج `Instance` من الأب.|**مستقل** تماماً.|
|**الذاكرة**|**Heavy:** شايل Reference خفي للأب (`this$0`).|**Light:** زيه زي أي كلاس عادي.|
|**Garbage Collection**|ممكن يمنع مسح الأب (Memory Leak Risk).|آمن جداً (Safe).|
|**الوصول للداتا**|بيشوف كل حاجة (Static & Non-static).|بيشوف الـ Static members بس.|

> [!TIP] **نصيحة سينيور:** لو الكلاس الداخلي بتاعك مش محتاج يوصل لبيانات (Instance Variables) من الكلاس الخارجي، **دايماً** خليه `static`. ده بيوفر ميموري وبيقلل التعقيد. ده اللي بيحصل مثلاً في `Node` بتاعة `LinkedList`.

---
# Lesson 5 (Part 4): Inner Classes (Types 3 & 4)

**(الكلاسات الداخلية - الجزء الثاني)**

#### 4. Type 3: Local Inner Class

**(النوع الثالث: الكلاس الداخلي المحلي)** **(Source:)**

##### 📖 The Surface (السطح)

ده كلاس بيتعرف **جوه Method**.

- **النطاق (Scope):** زي المتغير المحلي بالظبط. الكلاس ده "محبوس" جوه الميثود. أول ما الميثود تخلص وتوصل للـ closing brace `}`, الكلاس ده بيختفي ومحدش بره يقدر يشوفه.

##### 💻 Code Autopsy (تشريح الكود)

_(بناءً على Slide 242)_

```java
class Outer {
    void myMethod() {
        // متغير محلي (عايش في الـ Stack)
        final int number = 100;

        // تعريف الكلاس جوه الميثود!
        class LocalInner {
            void print() {
                // بيستخدم المتغير المحلي
                System.out.println("Number is: " + number);
            }
        }

        // لازم نستخدمه هنا حالاً، لأن بعد السطر ده الكلاس هيموت
        LocalInner local = new LocalInner();
        local.print();
    }
}
```

##### 🧠 The Deep Dive: The "Final" Rule (Stack vs. Heap War)

**(الغوص العميق: قاعدة الـ Final - حرب الذاكرة)** **(Source:)**

ليه المتغير `number` لازم يكون **`final`** (أو effectively final في النسخ الحديثة)؟ دي أهم نقطة في الدرس. ركز في دورة حياة الذاكرة:

1. **The Stack (المكدس):** لما `myMethod` تشتغل، المتغير `number` بيتخزن في الـ Stack. أول ما الميثود تخلص، الـ Stack Frame بيتدمر، و `number` **بيتمسح من الوجود**.
2. **The Heap (الكومة):** الأوبجيكت `local` (من نوع `LocalInner`) بيتخزن في الـ Heap. والـ Garbage Collector هو اللي بيقرر يمسحه إمتى (ممكن يفضل عايش بعد ما الميثود تخلص بكتير).
3. **The Conflict (النزاع):** لو الأوبجيكت `local` حاول ينادي `print()` بعد ما الميثود خلصت... هو بيحاول يوصل لمتغير `number` اللي "مات وشبع موت" في الـ Stack! 💀

**✅ الحل السينيور (The Solution):** الجافا بتعمل حركة خبيثة. هي مش بتخلي الـ Inner Class يشوف المتغير الأصلي. هي بتاخد **نسخة (Copy)** من قيمة `number` وتخزنها جوه الأوبجيكت في الـ Heap.

- عشان تضمن إن "النسخة" اللي جوه الأوبجيكت و "الأصل" اللي في الـ Stack مبيختلفوش عن بعض أبداً، الجافا بتشترط إن المتغير يكون **`final`** (ممنوع التغيير).

---

#### 5. Type 4: Anonymous Inner Class

**(النوع الرابع: الكلاس الداخلي المجهول)** **(Source:,)**

##### 📖 The Surface (السطح)

ده أشهر نوع قبل ظهور Java 8.

- **التعريف:** هو كلاس **بدون اسم**.
- **الاستخدام:** بنستخدمه لما نكون عايزين نعمل `Override` لميثود واحدة في السريع، ونعمل `new Object` في نفس اللحظة. (ضرب عصفورين بحجر).
- **مكان الشهرة:** بيستخدم بكثرة في الـ **Callbacks** والـ Event Handling (زي زراير الـ GUI).

##### 💻 Code Autopsy: The Timer Example

**(تشريح كود المؤقت - Slide 244)**

```java
import java.awt.event.ActionListener;
import java.awt.event.ActionEvent;
import javax.swing.Timer;

public class AnonymousDemo {
    public static void main(String[] args) {
        // الهدف: عمل مؤقت يضرب جرس كل 10 ثواني

        // هنا السحر: new InterfaceName() { ... }
        // إحنا مش بنعمل أوبجيكت من الإنترفيس (ده مستحيل)
        // إحنا بنعمل أوبجيكت من "كلاس مجهول" بينفذ الإنترفيس ده
        Timer t = new Timer(10000, new ActionListener() {

            // Override للميثود المطلوبة
            public void actionPerformed(ActionEvent e) {
                System.out.println("Beep!");
            }
        });

        t.start();
    }
}
```

##### 🧠 The Deep Dive: The `$1` Class

**(الغوص العميق: الكلاس رقم 1)** **(Source:)** بما إن الكلاس ملوش اسم، الكومبايلر بيعمل إيه؟ بيدي للكلاس ده **رقم**. لو بصيت في فولدر الـ `bin` بعد الـ Compilation، هتلاقي ملفات اسمها:

1. `AnonymousDemo.class` (الكلاس الرئيسي).
2. `AnonymousDemo$1.class` (أول كلاس مجهول قابله الكومبايلر).
3. `AnonymousDemo$2.class` (تاني واحد.. وهكذا).

##### 👨‍🏫 Senior Touch: The Lambda Evolution

**(لمسة السينيور: تطور اللامبدا)** **(Source:)**

> [!TIP] **Lambda vs Anonymous** في **Java 8**، الكود "القبيح" بتاع الـ Anonymous Class تم استبداله بالـ **Lambda Expression** لو كان الإنترفيس فيه ميثود واحدة (Functional Interface).
> 
> **قبل:** `new ActionListener() { public void actionPerformed(ActionEvent e) { ... } }`
> 
> **بعد (Lambda):** `e -> System.out.println("Beep!")`
> 
> **⚠️ تحذير:** الـ Anonymous Class لسه عايش ومهم! اللامبدا بتنفع بس لو ميثود واحدة. لو مضطر تعمل Override لـ **2 ميثودز** أو أكتر (مش Functional Interface)، لازم تستخدم الـ Anonymous Class التقليدي.

---



