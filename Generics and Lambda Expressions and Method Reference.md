# Part 1: The Pre-Generic Era (The "Object" Soup)

**(عصر ما قبل الجينيريكس - شوربة الكائنات)**

#### 1. The History: Why did we suffer?

**(التاريخ: لماذا عانينا؟)** **(Source:,)**

##### 📖 The Surface (السطح)

قبل Java 5 (سنة 2004)، الجافا كانت لغة "غير آمنة" فيما يخص الـ Collections. الـ `ArrayList` كانت مصممة إنها تشيل أي حاجة من نوع `Object`. وبما إن كل كلاس في الجافا بيورث من `Object`، كان المبرمج يقدر يحط "سمك لبن تمر هندي" في نفس الليستة، والكومبايلر واقف يتفرج ومبيعملش أي حاجة.

#### 2. The Problem: The Mystery Box

**(المشكلة: الصندوق الغامض)** **(Source:)**

##### 🧠 The Deep Dive (الغوص العميق)

تخيل الـ `ArrayList` زمان كأنها "صندوق أسود" (Mystery Box).

- أنت ممكن تحط فيه `String`.
    
- زميلك يحط فيه `File`.
    
- لما تيجي تسحب الداتا، أنت مش عارف إيه اللي طالعلك. لازم "تخمن" وتعمل **Casting**.
    
- لو التخمين غلط؟ 💥 **ClassCastException** في وش العميل (Runtime Crash).
    

##### 💻 Code Autopsy: The "Dark Ages"

**(تشريح كود العصور المظلمة - Slide 305 Logic)** **(Source:,)**

ده الكود اللي كان بيتكتب زمان، وركز في كل سطر ليه هو "خطر":

```java
// 1. القائمة بتقبل أي حاجة (Unsafe)
// مفيش أقواس < >، يعني هي Raw Type
ArrayList list = new ArrayList();

// 2. ضيفنا String (عادي لأن String هو Object)
list.add("Hello");

// 3. الكارثة: ضيفنا Integer بالغلط!
// الكومبايلر ساكت لأن Integer برضه Object
list.add(123);

// 4. جينا نسحب الداتا... ومفكرين إن كله String
// (String) -> دي عملية الـ Casting (التخمين)
String s = (String) list.get(1);
// 💥 Boom! ClassCastException
// البرنامج بيضرب لأننا بنحاول نحول (123) لـ String غصب واقتدار.
```

#### 3. Senior Analogy: The Airport Luggage 🧳

**(تشبيه السينيور: عفش المطار)** **(Source:)**

عشان تفهم الفرق بين النظام القديم والجديد، المصدر بيستخدم تشبيه عبقري:

- **The Old Way (بدون Generics):**
    
    - تخيل سير الحقائب في المطار. أي مسافر مسموح له يرمي **أي حاجة** على السير.
        
    - واحد رمى شنطة سوداء، والتاني رمى كرتونة بيض، والتالت رمى كيس زبالة.
        
    - أنت واقف مستني شنطتك. سحبت "حاجة" شكلها زي شنطتك، واضطريت تفتحها عشان تتأكد.
        
    - المفاجأة: طلعت كرتونة حد تاني! شكلك بقى وحش جداً (Exception).
        
- **The Generic Way (مع Generics):**
    
    - إدارة المطار حطت يافطة كبيرة: **"RED SUITCASES ONLY"** (حقائب حمراء فقط).
        
    - فيه "بودي جارد" (الـ Compiler) واقف عند المدخل. أي حد معاه كرتونة أو شنطة زرقاء بيطرده **قبل ما يدخل أصلاً** (Compile-time Error).
        
    - لما تيجي تسحب شنطة من السير، أنت **واثق 100%** إنها "حقيبة حمراء" من غير ما تفتحها وتعمل Check.
        

---

# Part 2: The Generic Solution (`<T>`) & Type Erasure

**(الحل الجينيرك ومسح النوع)**

#### 1. The Solution: Parameterized Types (`<T>`)

**(الحل: الأنواع ذات المعاملات)** **(Source:)**

##### 📖 The Surface (السطح)

بدل ما نسيب الـ List عايمة (`Raw Type`)، الجافا 5 قدمت الأقواس `< >`. دلوقتي أنت بتعرف "نوع" الداتا كأنه "باراميتر" للكلاس، زي ما بتبعت قيمة باراميتر للميثود.

- **قبل:** `ArrayList list = new ArrayList();` (أي حاجة تمشي).
    
- **بعد:** `ArrayList<String> list = new ArrayList<>();` (ممنوع دخول غير الـ Strings).
    

##### 💻 Code Autopsy: The Compiler Guard

**(تشريح الكود: حارس المترجم - Slide 307)**

```java
// 1. العقد الصريح (The Contract)
// إحنا بلغنا الكومبايلر: "الليستة دي بتاعة Strings بس"
List<String> list = new ArrayList<>();

// 2. التنفيذ الآمن
list.add("Hello"); // ✅ تمام

// 3. محاولة الاختراق
// list.add(123);
// ❌ Compile-time Error!
// الكومبايلر هنا بيشتغل "Bodyguard". بيمنعك تعمل Build أصلاً.
// في النظام القديم، ده كان بيعدي ويضرب عند العميل (Runtime).

// 4. السحب بدون Casting
// بما إن الكومبايلر ضمن إن مفيش غير Strings جوه
// مش محتاجين نكتب (String) list.get(0)
String s = list.get(0); // ✅ Clean Code
```

---

#### 2. 🧠 The Deep Dive: Type Erasure (The Grand Trick)

**(الغوص العميق: خدعة مسح النوع)** **(Source:,)**

دي المعلومة اللي بتفرق سينيور عن جونيور. أنت ممكن تفتكر إن لما بتكتب `ArrayList<String>` و `ArrayList<Integer>`, الجافا بتروح تكرّيت 2 كلاس جداد في الميموري، واحد للسترينج وواحد للانتجر (زي ما C++ Templates بتعمل).

**الحقيقة الصادمة:** **الجافا مبتعملش كده.** الجافا بتستخدم تكنيك اسمه **Type Erasure** (مسح النوع).

##### كيف يعمل السحر؟ (How it works)

العملية بتتم على مرحلتين:

1. **Compile Time (وقت الترجمة):**
    
    - الكومبايلر بيشوف `<String>`.
        
    - بيعمل Check على كل سطر كود للتأكد إنك ملتزم بالنوع.
        
    - لو حاولت تحط `Integer`, يديك Error.
        
2. **Runtime (وقت التشغيل):**
    
    - الكومبايلر **بيمسح (Erases)** كل معلومات الـ Generics (`<String>`).
        
    - بيبدل كل الـ Types بـ `Object` (أو الـ Bound بتاعها).
        
    - بيحط هو الـ **Casts** اللازمة في الـ Bytecode بالنيابة عنك.
        

##### 💻 Code Autopsy: Source vs. Bytecode

**(المصدر مقابل الكود المترجم)**

**اللي أنت كتبته (Source Code):**

```java
List<String> list = new ArrayList<>();
list.add("Hello");
String s = list.get(0);
```

**اللي الـ JVM بيشوفه ويشغله فعلياً (After Erasure):**

```java
// لاحظ: <String> اختفت! رجعت Raw Type
List list = new ArrayList();
list.add("Hello");
// الكومبايلر حط الـ Cast ده أوتوماتيك
String s = (String) list.get(0);
```

---

#### 3. 👨‍🏫 Senior Analysis: Why Erasure?

**(تحليل السينيور: ليه اللفة دي؟)** **(Source:)**

ليه الجافا معملتش Reification (إن الأنواع تكون حقيقية في الرن تايم) زي C#؟

السبب هو **Backward Compatibility** (التوافق الرجعي).

- لما Java 5 نزلت، كان فيه ملايين الأسطر مكتوبة بـ Java 1.4 (بدون Generics).
    
- لو الجافا غيرت طريقة عمل `ArrayList` في الرن تايم، كل المكتبات القديمة كانت هتضرب وتعمل Crash.
    
- **الحل العبقري:** نخلي التغيير كله في الـ Compiler بس، لكن الـ Bytecode يفضل زي ما هو عشان القديم والجديد يشتغلوا مع بعض في نفس المشروع.
    

> [!note]  
> **معلومة إضافية عن الأداء—invokedynamic و Lambdas:**
> 
> إضافة الـ Lambdas إلى جافا لم تكن مجرد "سكر نحوي"؛ الكومبايلر استخدم تعليمات بايتكود متقدمة مثل `invokedynamic` عشان ينشئ تطبيقات للامبدا بدون إنشاء ملفات `.class` لكل لامبدا كما كان يحدث مع Anonymous Inner Classes. النتيجة: كود أخف وأداء أحسن في كثير من الحالات. هذه الميزة ظهرت في ملفات الجافا 8 وما بعده كجزء من تحسينات الـ JVM للـ functional features.

---

# Part 3: Wildcards & Polymorphism (`? extends`)

**(أحرف البدل وتعدد الأشكال)**

#### 1. The Polymorphism Paradox (The Trap) 🪤

**(معضلة تعدد الأشكال - الفخ)** **(Source:)**

##### 📖 The Surface (السطح)

في الـ OOP العادي، لو `Circle` ابن لـ `Shape`, يبقى `Circle` يقدر يقعد مكان `Shape` عادي. دي بديهيات. بس في الـ Generics، المنطق ده **بيتعطل**.

- **سؤال:** هل `List<Circle>` تعتبر ابن لـ `List<Shape>`؟
    
- **الإجابة الصادمة:** **لأ (NO)!** 🛑
    

##### 🧠 The Deep Dive: Why? (Logic Check)

**(Source:)** لو الجافا سمحت بده، هيحصل كارثة في الـ Runtime. تخيل السيناريو ده:

```java
List<Circle> circles = new ArrayList<>();
// لو الجافا سمحت بالسطر ده:
List<Shape> shapes = circles;
// يبقى نقدر نضيف مربع (لأنه Shape) جوه ليستة الدوائر!
shapes.add(new Rectangle()); // 💥 كارثة! مربع وسط الدوائر.
```

عشان كده الجافا بتمنع ده (Invariance). `List<Shape>` لا تقبل إلا `Shape` بالظبط.

---

#### 2. The Solution: Wildcards (`?`)

**(الحل: علامة الاستفهام)** **(Source:,,)**

عشان نعمل ميثود مرنة تقبل "أي نوع من الأشكال", بنستخدم **Wildcard** مع **Upper Bound**. الصيغة: `List<? extends Shape>`. المعنى: "أنا بقبل ليستة لحاجة مجهولة (Unknown)، بس بشرط تكون وارثة من `Shape`".

#### 3. 👨‍🏫 Senior Rule: The PECS Principle 🌟

**(قاعدة بيكس الذهبية)** **(Source:,)**

دي نصيحة من المعماري Joshua Bloch (مؤلف كتاب _Effective Java_). امتى تستخدم `extends` وامتى `super`؟ احفظ الكلمة دي: **PECS**.

1. **P**roducer **E**xtends:
    
    - لو الليستة **بتنتج** داتا (أنت بتقرأ منها `get`).
        
    - استخدم `? extends T`.
        
    - _مثال:_ ميثود بتاخد ليستة أشكال عشان ترسمها (بتقرأ الأشكال).
        
2. **C**onsumer **S**uper:
    
    - لو الليستة **بتستهلك** داتا (أنت بتكتب فيها `add`).
        
    - استخدم `? super T`.
        
    - _مثال:_ ميثود بتاخد ليستة عشان تملاها داتا جديدة.
        

---

#### 4. 🧪 Lab Exercise 1: The Shape Hierarchy

**(تمرين المعمل 1: هرمية الأشكال)** **(Source:,,)**

> **المهمة:** اكتب ميثود `drawPics` تاخد قائمة أشكال وترسمهم، ولازم تقبل `ArrayList<Rectangle>` و `ArrayList<Circle>`.

##### 💻 Code Autopsy: The Implementation

```java
// 1. الأب المجرد
abstract class Shape {
    abstract void draw();
}

// 2. الأبناء
class Rectangle extends Shape {
    void draw() { System.out.println("[]"); }
}

class Circle extends Shape {
    void draw() { System.out.println("O"); }
}

// 3. التطبيق (Logic)
public class ArtStudio {

    // ❌ طريقة الهواة: public void drawPics(List<Shape> shapes)
    // ده غلط لأنه مش هيقبل List<Circle> بسبب مشكلة الـ Invariance

    // ✅ طريقة السينيور (PECS: Producer Extends)
    // بنستخدم Wildcard لأننا بنقرأ (Produce) من الليستة عشان نرسم
    public void drawPics(List<? extends Shape> shapes) {
        for (Shape s : shapes) {
            s.draw(); // آمنة 100% لأن أي حاجة هنا هي أكيد Shape
        }
    }
}
```

---

# Part 4: Generic Math & Intro to Lambdas

**(الرياضيات المعممة وبداية اللامبدا)**

#### 1. 🧪 Lab 2: The Generic Complex Number (The Trap) 🪤

**(تمرين المعمل 2: الأعداد المركبة والفخ)** **(Source:,)**

> **المهمة:** صمم كلاس `Complex<T>` يمثل الأعداد المركبة ($a + bi$)، واعمل ميثود للجمع `add()`.

##### 🧠 The Senior Analysis (The Math Problem)

لو كتبت `class Complex<T>` وحاولت تجمع `T + T`... الكومبايلر هيضربك على إيدك. 👋 الجافا متعرفش `T` ده إيه. ممكن يكون `String` أو `Scanner`. ومينفعش نجمع `Scanner + Scanner`.

**الحل المعماري:**

1. **Bounded Type:** لازم نحدد إن `T` هو نوع رقمي: `<T extends Number>`.
    
2. **Double Conversion:** الجافا معندهاش Generic Math Operators (عكس C++). الحل الوحيد إننا نحول القيم لـ `double` ونجمعها.
    

##### 💻 Code Autopsy: The Solution

```java
// 1. القيد: T لازم يكون ابن لـ Number
class Complex<T extends Number> {
    T real;
    T imag;

    public Complex(T real, T imag) {
        this.real = real;
        this.imag = imag;
    }

    // 2. ميثود الجمع
    // لاحظ: بنرجع Complex<Double> لأننا مضطرين نحول لـ double
    public Complex<Double> add(Complex<?> other) {
        // فخ التحويل: لازم نستخدم .doubleValue()
        // الميثود دي مضمونة الوجود لأننا عملنا extends Number
        double newReal = this.real.doubleValue() + other.real.doubleValue();
        double newImag = this.imag.doubleValue() + other.imag.doubleValue();

        return new Complex<>(newReal, newImag); // Type Inference بيشتغل هنا
    }
}
```

---

### إضافة مترجمة: دالة الطرح (subtract)

في الملف الإنجليزي تم شرح واحتواء ميثود `subtract` كذلك. نضيف هنا ترجمة ومثال لطريقة الطرح لتكملة الدعم العملي لكلاس الـ `Complex`.

```java
// Generic Method: Subtraction
public Complex<Double> subtract(Complex<?> other) {
    double newReal = this.real.doubleValue() - other.real.doubleValue();
    double newImag = this.imag.doubleValue() - other.imag.doubleValue();

    return new Complex<>(newReal, newImag);
}
```

> [!note]  
> **ملاحظة:** السبب في استخدام `doubleValue()` هو أن `Number` يُوفر هذه الطريقة، ولا يوجد في Java طريقة عامة لعمل العمليات الرياضية على النوع العام `T`.

---

### Lesson 8: Lambda Expressions (The Revolution) 🚀

**(تعبيرات اللامبدا - الثورة)** **(Source:)**

هنا بنبدأ نقلة نوعية في الجافا. الانتقال من **OOP** (كائنات) لـ **Functional Programming** (سلوك).

#### 2. The Concept: Code as Data

**(المفهوم: الكود كبيانات)** **(Source:)**

##### 📖 The Surface (السطح)

في الـ OOP الصارم، مينفعش تبعت "ميثود" كباراميتر لميثود تانية. لازم تبعت "Object". الـ **Lambda** كسرت القاعدة دي. هي سمحتلك تبعت "بلوك كود" (Block of Code) كأنه متغير عادي.

#### 3. 🚌 Senior Analogy: The Butler vs. The Note

**(تشبيه السينيور: الخادم مقابل الورقة)** **(Source:,)**

التشبيه ده بيلخص الفرق بين Java 7 (Anonymous Inner Classes) و Java 8 (Lambdas):

تخيل إنك مسافر وعايز صاحبك يسقي الزرع:

1. **The Old Way (Anonymous Inner Class):**
    
    - مينفعش تتصل تقوله "اسقي الزرع".
        
    - لازم تأجر **خادم** (Create Class).
        
    - تعلم الخادم إزاي يسقي الزرع (Override Method).
        
    - تبعت الخادم لبيت صاحبك (Pass Object).
        
    - _مجهود رهيب (Boilerplate) عشان مهمة تافهة!_
        
2. **The Lambda Way:**
    
    - بتكتب ورقة ملاحظات (Sticky Note): **"من فضلك اسقي الزرع"**.
        
    - بتلزقها لصاحبك. انتهى.
        
    - **القاعدة:** Lambdas remove the Butler. (اللامبدا بتلغي الحاجة للكلاس الوسيط).
        

---

# Part 5: Block Lambdas & Generic Functional Interfaces

**(لامبدا الكتلية والواجهات المعممة)**

#### 1. Block Lambda Expressions `{ ... }`

**(تعبيرات لامبدا الكتلية - الكود المعقد)** **(Source:)**

##### 📖 The Surface (السطح)

لحد دلوقتي كل أمثلتنا كانت سطر واحد كيوت زي: `() -> 98.6`. بس الحياة الواقعية مش بالسهولة دي. ساعات اللوجيك بتاعك بيحتاج `Loop`، أو `if-else` معقدة، أو تعريف متغيرات مؤقتة. هنا الجافا بتقولك: "عايز مساحة؟ خد راحتك، بس بشروط صارمة". ده اسمه **Block Body**.

##### 💻 Code Autopsy: Finding the Smallest Factor

**(تشريح الكود: البحث عن أصغر معامل)** **(Source:,)**

الهدف: عايزين ميثود تاخد رقم `n` وترجع أصغر رقم يقبل القسمة عليه (غير الواحد). ده مستحيل يتكتب في سطر واحد (Single Expression).

```java
interface NumericFunc {
    int func(int n);
}

public class BlockLambdaDemo {
    public static void main(String[] args) {

        // Block Lambda: لاحظ استخدام الأقواس {}
        // بما إننا فتحنا قوس، إحنا فتحنا "عالم جديد"
        NumericFunc smallestFactor = (n) -> {
            int result = 1;

            // محتاجين Loop عشان نجرب الأرقام
            // بنبدأ من 2 لحد ما نلاقي المعامل
            n = (n < 0) ? -n : n; // لو سالب اقلبه موجب

            for(int i = 2; i <= n/i; i++) {
                if ((n % i) == 0) {
                    result = i;
                    break; // لقينا المعامل، اخرج من اللوب
                }
            }
            // ⚠️ الشرط القاتل: لازم تكتب return صريحة
            return result;
        }; // ولازم semicolon في قفلة اللامبدا

        System.out.println("Smallest factor of 12 is " + smallestFactor.func(12));
    }
}
```

##### 🧠 The Deep Dive: The Syntax Rules (The Senior Trap) 🪤

**(الغوص العميق: قواعد النحو - فخ السينيور)** **(Source:)**

فيه فرق جوهري بين الطريقتين، ولو خلطت بينهم الكود مش هيعمل Compile. ركز عشان دي بتوقع ناس كتير في الـ Code Review:

|وجه المقارنة|Single Expression (سطر واحد)|Block Body (بلوك كامل)|
|:--|:--|:--|
|**الشكل**|`(n) -> n * n`|`(n) -> { return n * n; }`|
|**الأقواس {}**|ممنوع تكتبها (إلا لو قلبت بلوك).|**إجباري** تكتبها.|
|**الـ Return**|ممنوع تكتب كلمة `return`. (ضمني).|**إجباري** تكتب `return` (لو الميثود بترجع قيمة).|
|**الـ Semicolon**|ممنوع `;` جوه السطر.|**إجباري** تنهي كل سطر بـ `;`.|

> [!WARNING] **The Void Trap (فخ الفراغ)** **(Source:)**
> 
> - لو الإنترفيس نوعه `void` (مبيرجعش حاجة)، عادي تستخدم `{}` من غير `return`.
>     
> - لكن لو الإنترفيس نوعه `int` (زي المثال اللي فوق)، ونسيت تكتب `return` جوه الـ `{}`، الكومبايلر هيصرخ في وشك: **"Missing return statement"**.
>     

---

#### 2. Generic Functional Interfaces `<T>`

**(الواجهات الوظيفية العامة)** **(Source:)**

##### 📖 The Surface (السطح)

في المثال اللي فات، `NumericFunc` كانت شغالة مع `int` بس. طب لو عايزين نعمل Test لـ `String`؟ هل نعمل `StringTest`؟ ولـ `Double` نعمل `DoubleTest`؟ لأ طبعاً. إحنا مبرمجين، مش طابعين. الحل نستخدم **Generics `<T>`** عشان نعمل "قالب" واحد يشتغل مع أي نوع داتا.

##### 💻 Code Autopsy: The Universal Tester

**(تشريح الكود: المختبر الشامل)** **(Source:,)**

```java
// T هو النوع العام (Variable Type)
// الإنترفيس ده يقبل أي نوعين ويقارن بينهم
interface SomeTest<T> {
    boolean test(T n, T m);
}

class GenericLambdaDemo {
    public static void main(String[] args) {

        // Case 1: Integers (Math Logic)
        // الكومبايلر استنتج إن T = Integer
        SomeTest<Integer> isFactor = (n, d) -> (n % d) == 0;

        // Case 2: Strings (Text Logic)
        // الكومبايلر استنتج إن T = String
        // عشان كده سمح لنا نستخدم indexOf (ميثود خاصة بالسترينج)
        SomeTest<String> isIn = (a, b) -> a.indexOf(b) != -1;

        // Case 3: Doubles (Precision Logic)
        SomeTest<Double> isEqual = (a, b) -> Math.abs(a - b) < 0.0001;

        if(isIn.test("Generic Power", "Power"))
            System.out.println("Found!");
    }
}
```

##### 🧠 Senior Analysis: Type Inference vs Erasure

**(تحليل السينيور: الاستنتاج ضد المسح)** **(Source:)**

إزاي الجافا بتفهم الكود ده؟

1. **Compile Time (وقت الترجمة):** لما كتبت `SomeTest<String>`, الكومبايلر ضمن إن المتغيرات `a` و `b` جوه اللامبدا هي من نوع `String`.
    
    - الدليل: لو جيت جوه اللامبدا وكتبت `a / b` (قسمة)، هيديك Error فوراً، لأن السترينج مبيتسقمش. ده اسمه **Type Safety**.
        
2. **Runtime (وقت التشغيل):** الجافا بتطبق الـ **Erasure** وبتحول كله لـ `Object`, بس إحنا مش خايفين من الـ `ClassCastException` لأن الكومبايلر قام بالواجب خلاص.
    

---

# Part 6: Method References & Final Labs

**(مراجع الطرق والتمارين النهائية)**

#### 1. What is a Method Reference? (`::`)

##### 📖 The Surface (السطح)

الـ **Method Reference** هو مجرد "اختصار" (Shorthand) للامبدا. لو اللامبدا بتاعتك وظيفتها الوحيدة إنها تاخد الداتا وتبعتها لميثود تانية زي ما هي، من غير أي تغيير... يبقى ليه نكتب اللامبدا أصلاً؟ الجافا بتقولك: "شاور على الميثود بس، وأنا هتصرف".

##### 🧠 The Deep Dive: The "Pass-Through" Logic

**(الغوص العميق: منطق التمرير)** **(Source:)** فكر فيها كأنها "أنبوبة توصيل".

- **Lambda:** `x -> System.out.println(x)` (أنت بتستلم `x` وبعدين تبعتها).
    
- **Method Ref:** `System.out::println` (أنت بتقول للجافا: وصل الداتا للميثود دي فوراً).
    

##### 🚌 Senior Analogy: The Restaurant Menu 🍳

**(تشبيه السينيور: قائمة المطعم)** **(Source:)** التشبيه ده من المصدر عشان يوضح الفرق في "التجريد" (Abstraction):

- **Lambda (الوصفة):** أنت بتكتب ورقة للشيف: _"هات البيضة، اكسرها، حطها في الزيت، اقليها"_.
    
- **Method Reference (المنيو):** أنت بتشاور في المنيو وتقول: **"بيض مقلي"**.
    
    - _الشيف (الكومبايلر) عارف الوصفة مسبقاً، مش محتاج تشرحهاله._
        

---

#### 2. The Evolution of Syntax (3 Stages)

**(تطور بناء الجملة - المراحل الثلاث)** **(Source:)**

المصدر بيقسم تطور الجافا في الحتة دي لـ 3 عصور (زي التاريخ):

1. **The Stone Age (Anonymous Inner Class):**
    
    - كود ضخم، ملفات `.class` زيادة، وأداء أبطأ.
        
2. **The Industrial Age (Lambda Expression):**
    
    - `f -> f.isDirectory()`
        
    - شيلنا الدوشة (Boilerplate)، بس لسه بنكتب المتغير `f` مرتين (Redundancy).
        
3. **The Space Age (Method Reference):** 🚀
    
    - `File::isDirectory`
        
    - حذفنا المتغير تماماً. الكود بقى يتقري كأنه جملة إنجليزي: _"List files that are directories"_.
        

---

#### 3. The 4 Types of Method References (The Core)

**(الأنواع الأربعة - الجوهر التقني)** **(Source:,,)**

دي أهم نقطة في الدرس، ولازم تفرق بينهم لأن الـ Syntax واحد `::` بس المعنى بيختلف حسب السياق.

##### 📊 The Senior Summary Chart

**(جدول السينيور)** _(بناءً على Source 36)_

|النوع (Type)|الصيغة (Syntax)|المقابل باللامبدا (Lambda Equivalent)|شرح السينيور (Senior Note)|
|:--|:--|:--|:--|
|**A. Static**|`Ref::staticMethod`|`(x) -> Ref.staticMethod(x)`|**سهل:** نداء مباشر لدالة ستاتيك.|
|**B. Specific Object**|`myObj::method`|`(x) -> myObj.method(x)`|**محدد:** بستخدم أوبجيكت موجود عندي _بره_ اللامبدا.|
|**C. Arbitrary Object**|`Class::method`|`(x, y) -> x.method(y)`|**خادع:** الميثود بتتنفذ على _أول معامل_ (Input) بيدخل اللامبدا.|
|**D. Constructor**|`Class::new`|`(x) -> new Class(x)`|**مصنع:** لإنشاء كائنات جديدة (Factory Pattern).|

##### 💻 Code Autopsy: The Tricky Types

**(تشريح الكود للأنواع الخادعة)**

**Type B: Specific Object (Slide 349)**

```java
String prefix = "Mr. ";
// هنا بننادي ميثود على المتغير prefix اللي عرفناه فوق
// اللامبدا: (name) -> prefix.concat(name)
Function<String, String> addTitle = prefix::concat;
```

**Type C: Arbitrary Object (Slide 350) ⚠️**

```java
// هنا بننادي method موجودة في كلاس String
// بس مش على متغير عندنا.. لا.. على المتغير اللي "هيدخل" اللامبدا
// اللامبدا: (str, other) -> str.compareToIgnoreCase(other)
Comparator<String> cmp = String::compareToIgnoreCase;
```

- **ليه سموه Arbitrary (عشوائي/أي كائن)؟** لأنك بتقول: "أي سترينج يجيلي، نفذ عليه الميثود دي".
    

---

#### 4. Final Lab Exercises 🧪

**(تمارين المعمل النهائية)** **(Source:)**

المصدر بيختم بمسألتين للتأكد من الفهم.

##### Lab 1: The "Better String"

**(السترينج الأفضل)** **(Source:)**

> **المهمة:** اكتب ميثود `betterString` تاخد اتنين String ولامبدا (الشرط)، وترجع السترينج اللي يكسب.

```java
// 1. الواجهة الوظيفية (The Judge)
interface StringPredicate {
    boolean test(String s1, String s2);
}

public class Utils {
    // 2. الميثود الرئيسية
    public static String betterString(String s1, String s2, StringPredicate p) {
        // لو الشرط تحقق رجع s1، وإلا رجع s2
        if (p.test(s1, s2)) return s1;
        else return s2;
    }

    public static void main(String[] args) {
        String s1 = "Hello";
        String s2 = "Java Is Cool";

        // Challenge 1: مين الأطول؟
        // اللامبدا: (a, b) -> a.length() > b.length()
        String longer = betterString(s1, s2, (a, b) -> a.length() > b.length());

        // Challenge 2: مين الأول دايماً؟
        String first = betterString(s1, s2, (a, b) -> true);
    }
}
```

##### Lab 2: The "Alphabet Checker"

**(فاحص الحروف الأبجدية)** **(Source:)**

> **المهمة:** تأكد إن النص يحتوي على حروف فقط (Alphabets) باستخدام `Method Reference`.

```java
public class LetterCheck {
    public static boolean isAllLetters(String s) {
        // الحل باستخدام Java 8 Method References
        // ملاحظة: المصدر بيستخدم اللوب العادية لأن Streams لسه الدرس الجاي
        for (int i = 0; i < s.length(); i++) {
            // بدل ما نكتب: char c -> Character.isLetter(c)
            // بنستخدم Static Method Reference:
            if (!Character.isLetter(s.charAt(i))) {
                return false;
            }
        }
        return true;
    }
}
```

---

### إضافات مترجمة ومطابقة للمحتوى الإنجليزي (مدمجة في المواضع المناسبة):

**1. لمحة تاريخية عن اسم "Lambda" وأصل الفكرة**

اسم الـ "Lambda" جاي من عالم الرياضيات والمنطق — **Alonzo Church** ومفهوم الـ _lambda calculus_ اللي وصف طريقة رسمية للتعبير عن الحساب والوظائف. تصميم الـ Lambdas في جافا مُستلهم من هذا الأساس النظري، وأدخلته جافا بعناية عشان تضيف قدرات نمطية Functional Programming من غير ما تكسر الـ OOP القائم.

**2. ملاحظة تصميمية حول الأداء والـ invokedynamic**

الانتقال من Anonymous Inner Classes للـ Lambdas لم يكن مجرد تغيير في الشكل. الكومبايلر وجهاز الـ JVM استغلا آلية اسمها `invokedynamic` لتقليل الإنشاءات الثقيلة للكائنات ولتأجيل توليد تطبيقات اللومبدا إلى وقت التشغيل بطريقة أكثر كفاءة. النتيجة: استهلاك ذاكرة أقل وأحياناً وقت تنفيذ أفضل.

**3. جدول مساعد: متى تستخدم Lambda، Method Reference، أم Block Lambda**

|الحالة|الأسلوب المفضل|لماذا|
|:--|:--|:--|
|نداء طريقة جاهزة بدون تعديل|Method Reference (`Class::method`)|أقصر وأوضح ويقرأ كجملة إنجليزية|
|لوجيك بسيط ورد في سطر واحد|Single-expression Lambda|مختصر وسهل القراءة|
|لوجيك معقد يحتاج حلقات/فرع|Block Lambda `{ ... }`|ضروري لو في إرجاع بعد عدة سطور|

**4. مثال بديل سريع باستخدام Streams لفحص الأحرف**

في الملف الإنجليزي ذُكرت طريقة Streams كـ "Pro Tip". نضيف هنا طريقة بديلة أسرع وأقصر باستخدام Streams (لمحة):

```java
public static boolean isPureTextStream(String s) {
    return s.chars().allMatch(Character::isLetter);
}
```

هذا المثال يستغل أن `s.chars()` يعطي Stream من القيم الصحيحة (int) ويمكن تطبيق `allMatch` مع مرجع الميثود `Character::isLetter`.

**5. ملاحظة عن الـ Type Erasure وتأثيره على الـ Reflection**

الـ Type Erasure يجعل معلومات الأنواع العامة غير متاحة في وقت التشغيل. لو احتجت تعرف النوع الحقيقي لباراميتر Generic عبر الـ Reflection، هتلاقي صعوبات لأن المعلومة دي ممسوحة. لذلك، مكتبات الـ serialization أو frameworks الكبيرة (مثل بعض أجزاء من Spring أو Gson) تستخدم طرق إضافية (TypeToken، ParameterizedType) عشان تعيد تركيب معلومات الأنواع.

**6. ملخص سريع لأنواع Method References (مترجم وموسع)**

- **Static method reference (`Class::staticMethod`)**: بندي دالة ستاتيك اسمها.
    
- **Reference to a specific object (`myObj::method`)**: بندي ميثود تابعة لكائن موجود.
    
- **Reference to an arbitrary object (`Class::method`)**: الجملة تعني "اطلب هذه الميثود من الكائن الذي سيأتي كمدخل".
    
- **Constructor reference (`Class::new`)**: اختصار أنشئ كائن جديد.
    

---

## خاتمة سريعة (ملاحظات عملية)

- استخدم Generics عشان تحافظ على Type Safety وتقلل الأخطاء في وقت التشغيل.
    
- فهم قواعد الـ Wildcards (`extends` مقابل `super`) مهم جداً عند تصميم APIs.
    
- Lambdas و Method References يسهلوا كتابة الكود ويقصروا الـ boilerplate. استغلهم مع واجهات وظيفية (Functional Interfaces).
    
- كن واعياً للـ Type Erasure عندما تحتاج معلومات الـ Types في وقت التشغيل.