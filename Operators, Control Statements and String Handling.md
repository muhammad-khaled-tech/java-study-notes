
# Lesson 3 (Part 1): Operators & Type Checking

**(الدرس 3 - الجزء 1: المعاملات والتحقق من الأنواع)**

#### 1. Arithmetic Operators

**(المعاملات الحسابية)** **(Slide 100)**

##### 📖 The Surface (السطح)

دي أساسيات الحساب اللي اتعلمناها في المدرسة: `+`، `-`، `*`، `/`، `%` (باقي القسمة). وبتشمل كمان **الزيادة (Increment)** `++` و **النقصان (Decrement)** `--`.

##### 🧠 The Deep Dive (Integer Division) / (الغوص العميق: قسمة الأعداد الصحيحة)

خطأ شائع جداً للمبتدئين:

- في الرياضيات: 5 / 2 = 2.5
- في الجافا (أعداد صحيحة): 5 / 2 = 2 **ليه؟** لو الطرفين (operands) عبارة عن أعداد صحيحة (integers)، الجافا بتنفذ **قسمة صحيحة (Integer Division)**. هي "بتقص" (truncates) الجزء العشري. هي **لا** تقرب الرقم للأعلى.

##### 👨‍🏫 Senior Touch: The ++ Trap (لمسة السينيور: فخ الـ ++)

فيه فرق بين `x++` (Post-increment) و `++x` (Pre-increment).

- **`++x` (Pre):** زود القيمة الأول، _وبعدين_ استخدمها.
- **`x++` (Post):** استخدم القيمة الحالية الأول، _وبعدين_ زودها.

```java
int a = 10;
int b = ++a; // a becomes 11, then b becomes 11.
System.out.println("a: " + a + ", b: " + b); // Output: 11, 11

int x = 10;
int y = x++; // y becomes 10, then x becomes 11.
System.out.println("x: " + x + ", y: " + y); // Output: 11, 10
```

---

#### 2. Bitwise Operators (The Matrix Mode)

**(معاملات البت - وضع الماتريكس)** **(Slides 101 - 104)**

##### 📖 The Surface (السطح)

المعاملات دي بتشتغل مباشرة على **البتات (bits)** (الأصفار والوحايد) جوه الرقم الصحيح.

| المعامل | الاسم                | الوصف                                                         |
| :------ | :------------------- | :------------------------------------------------------------ |
| `~`     | Unary NOT            | بيقلب كل البتات (0 يبقى 1، والـ 1 يبقى 0).                    |
| `&`     | AND                  | بيرجع 1 لو البت في _الطرفين_ بـ 1.                            |
| \|      | or                   | OR                                                            |
| `^`     | XOR                  | بيرجع 1 لو البتات _مختلفة_ عن بعض.                            |
| `<<`    | Shift Right          | بيقسم على 2 (مع الحفاظ على إشارة الرقم).                      |
| `>>`    | Shift Left           | بيضرب في 2.                                                   |
| `>>>`   | Unsigned Right Shift | بيقسم على 2 (بيملأ الخانات اللي فضيت بأصفار، وبيهمل الإشارة). |
|         |                      |                                                               |
|         |                      |                                                               |

##### 🧠 The Deep Dive (Signed vs. Unsigned Shift) / (الغوص العميق: الإزاحة بإشارة وبدون)

الجافا بتستخدم نظام (Two's Complement) لتخزين الأرقام السالبة.

- **`>>` (Arithmetic Shift):** لو الرقم سالب (بيبدأ بـ 1)، المعامل ده بيسحب الـ 1 معاه وهو بيعمل إزاحة عشان يحافظ على الرقم سالب.
- **`>>>` (Logical Shift):** ده ملوش دعوة بالإشارة. هو دايماً بيملأ الناحية الشمال بأصفار. دي خاصية فريدة في الجافا (C++ مش بتعرفها بشكل صارم).

##### 💡 Analogy: The Bus Bench (تشبيه: دكة الأتوبيس)

تخيل دكة (byte) عليها 8 كراسي.

- **`<<` (Left Shift):** كل الناس بتزحف للشمال. الشخص اللي قاعد في أقصى الشمال بيقع. وبيظهر كرسي فاضي على اليمين (بيتملي بـ 0).
- **`>>` (Right Shift):** كل الناس بتزحف لليمين. الشخص اللي قاعد في أقصى اليمين بيقع.
    - _مين هيقعد في الكرسي الفاضي اللي ظهر على الشمال؟_
    - لو استخدمت `>>`: الشخص اللي _كان_ قاعد في أقصى الشمال بيستنسخ نفسه (بيحافظ على الإشارة).
    - لو استخدمت `>>>`: شبح (0) هو اللي بيقعد هناك.

```java
int a = 60; // 0011 1100
int b = 13; // 0000 1101

// a & b = 0000 1100 (12) -> Only 1s where BOTH are 1
// a | b = 0011 1101 (61) -> 1s where EITHER is 1
// a ^ b = 0011 0001 (49) -> 1s where they are DIFFERENT
```
---

#### 3. Relational Operators

**(معاملات العلاقة/المقارنة)** **(Slide 105)**

##### 📖 The Surface (السطح)

بتقارن بين قيمتين و **دايماً بترجع boolean** (يا true يا false). المعاملات هي: `==`، `!=`، `>`، `<`، `>=`، `<=`.

##### 👨‍🏫 Senior Touch / Pro Tips (لمسة السينيور)

> [!WARNING] **فخ الـ == (The == Trap)**
> 
> - للأنواع البدائية (int, char, boolean): الـ `==` بتقارن **القيم (Values)**.
> - للكائنات (Objects زي String, Scanner, Arrays): الـ `==` بتقارن **عناوين الذاكرة (Memory Addresses)**.
> 
> **خطأ جونيور:** `if (name == "Ahmed")` **تصحيح سينيور:** `if (name.equals("Ahmed"))`

---

#### 4. Boolean Logical Operators

**(المعاملات المنطقية)** **(Slides 106 - 107)**

##### 📖 The Surface (السطح)

تستخدم لدمج شروط متعددة.

|المعامل|الاسم|السلوك|
|:--|:--|:--|
|`&`|Logical AND|بيفحص الطرفين دايماً.|
|`|`|Logical OR|
|`&&`|**Short-circuit AND**|لو الشمال false، بيقف ومبيكملش فحص.|
|`|`|**Short-circuit OR**|
|`!`|NOT|بيقلب الـ true لـ false والعكس.|

##### 🧠 The Deep Dive: Short-Circuiting (&& vs &) / (الغوص العميق: الدائرة القصيرة)

ليه عندنا نوعين من الـ AND؟

- **الكفاءة (Efficiency):** الـ `&&` كسولة (lazy). لو الشرط A طلع False، مستحيل المعادلة `A && B` تطلع true. فـ الجافا بتوفر وقت ومبتعملش تقييم لـ B.
- **الأمان (Safety):** ده بيسمح لنا نكتب كود آمن (مثلاً: نتأكد إن الأوبجيكت مش null قبل ما ننادي ميثود جواه في نفس سطر الـ if).

```java
String str = null;

// CRASH! Java tries to check length() on null
// if (str != null & str.length() > 0)

// SAFE! Java sees str is null (false), so it skips the right side.
if (str != null && str.length() > 0) {
    System.out.println("String is valid");
}
```

(Slide 107)

```java
// Demonstrate the boolean logical operators.
class BoolLogic {
    public static void main(String args[]) {
        boolean a = true;
        boolean b = false;
        boolean c = a | b; // true
        boolean d = a & b; // false
        boolean e = a ^ b; // true (different)
        boolean f = (!a & b) | (a & !b); // (false & false) | (true & true) = true
        boolean g = !a; // false

        System.out.println("a = " + a);
        System.out.println("b = " + b);
        System.out.println("a|b = " + c);
        System.out.println("a&b = " + d);
        System.out.println("a^b = " + e);
        System.out.println("!a&b|a&!b = " + f);
        System.out.println("!a = " + g);
    }
}
```
---

#### 5. The Ternary Operator (? :)

**(المعامل الثلاثي)** **(Slide 108)**

##### 📖 The Surface (السطح)

هو اختصار لـ if-else. **النحو:** `condition ? value_if_true : value_if_false`

##### 💻 Code Autopsy (تشريح الكود)

- **الطريقة القديمة:** 4 سطور كود بـ if/else عشان تحدد قيمة متغير.
```java
double ratio;
if (total == 0) {
    ratio = 0;
} else {
    ratio = num / total;
}
```
- **طريقة السينيور (Ternary):** سطر واحد نضيف ومباشر.
```java
// Much cleaner!
double ratio = (total == 0) ? 0 : num / total;
```

---

#### 6. The instanceof Operator

**(معامل instanceof)** **(Slides 109 - 115)**

##### 📖 The Surface (السطح)

المعامل ده بيجاوب على سؤال: **"أنت نوعك إيه يا أوبجيكت؟"** أثناء تشغيل البرنامج (runtime). **النحو:** `objectref instanceof type` -> بترجع true أو false.

##### 🧠 The Deep Dive: Why do we need it? (الغوص العميق: ليه محتاجينه؟)

في تعدد الأشكال (Polymorphism)، متغير من نوع `Animal` ممكن يكون شايل في الميموري `Dog` أو `Cat`. قبل ما تحاول تخليه يهوهو `bark()`، لازم تتأكد إنه فعلاً `Dog`، وإلا هيضرب في وشك `ClassCastException`.

### 💻 Lecture Example Breakdown (Slide 112)
```java
class A { int i, j; }
class B { int i, j; }
class C extends A { int k; }
class D extends A { int k; }

class InstanceOf {
    public static void main(String args[]) {
        A a = new A();
        B b = new B();
        C c = new C();
        D d = new D();

        // 1. Direct Instance
        if(a instanceof A) System.out.println("a is instance of A"); // True

        // 2. Inheritance Check
        // c is an instance of C, BUT C is a child of A.
        // Therefore, c IS ALSO an instance of A.
        if(c instanceof A) System.out.println("c can be cast to A"); // True

        // 3. Cross-Family Check
        // a is a parent. It is NOT an instance of the child C.
        if(a instanceof C) System.out.println("a can be cast to C"); // False!

        // 4. Object Check
        // Everything in Java extends Object.
        if(d instanceof Object) System.out.println("d may be cast to Object"); // True
    }
}
```
##### 👨‍🏫 Senior Touch / Pro Tips (لمسة السينيور)

> [!TIP] **قاعدة التعددية (Polymorphism Rule)**
> 
> - **الابن instanceof الأب:** دايماً True. (الكلب هو حيوان).
> - **الأب instanceof الابن:** عادةً False (إلا لو متغير الأب ده شايل فعلياً أوبجيكت ابن).
> - **الأخ instanceof الأخ:** دايماً خطأ مترجم (Compiler Error) أو False. (الكلب عمره ما يكون قطة).

##### 💡 Analogy: The ID Check (تشبيه: فحص البطاقة)

- **أنت (Object C):** أنت "إنسان" (Class C).
- **الأب (Class A):** أنت برضه "ثدييات" (Class A).
- **الجد (Object):** أنت برضه "كائن حي" (Class Object).
- الـ `instanceof` بتفحص لو أنت بتنتمي لأي جروب من دول. `you instanceof Mammal` نتيجتها True. `you instanceof Reptile` نتيجتها False.

---

### Lesson 3 (Part 2): The Functional Revolution

**(الدرس 3 - الجزء 2: الثورة الوظيفية)**

> [!ABSTRACT] **نظرة عامة على المحاضرة** إحنا هنستكشف أكبر تغيير في الـ Syntax في تاريخ الجافا: **تعبيرات اللامبدا (Lambda Expressions)**. هنتعلم إزاي نكتب "ميثودز مجهولة" (anonymous methods) باستخدام معامل السهم `->` وإزاي نخلي الكود بتاعنا مختصر ومعبر.

**الشرائح:** 116 – 119 (التوقف قبل جمل التحكم).

#### 1. The Lambda Expression

**(تعبير اللامبدا)** **(Slide 116)**

##### 📖 The Surface (السطح)

- **التعريف:** تعبير اللامبدا هو أساساً **ميثود مجهولة** (ميثود من غير اسم).
- **القيد:** مش بتشتغل لوحدها. بتستخدم عشان تعمل Implement لميثود معرفة جوه **Functional Interface**.
- **Functional Interface:** دي Interface بتحتوي على **ميثود abstract واحدة فقط لا غير** (SAM: Single Abstract Method).

##### 🧠 The Deep Dive (The "Closure" Concept) / (الغوص العميق: مفهوم الإغلاق)

Slide 116 بتذكر إن اللامبدا بيتقال عليها **Closures**. يعني إيه؟ في الجافا القديمة، لو كنت عايز تمرر كود كبيانات (مثلاً: "نفذ الأكشن ده لما الزرار يتداس")، كنت بتضطر تعمل Anonymous Inner Class. ده كان بيعمل ملف `.class` منفصل على الهارد (مثلاً `Main$1.class`). **اللامبدا أذكى.** لما بتكتب لامبدا، المترجم بيستخدم آلية اسمها `invokedynamic` (نزلت في Java 7). هو مش بالضرورة بيعمل ملف class تقيل. هو بيقول للـ JVM: _"بقولك إيه، اعتبري بلوك الكود الصغير ده هو تنفيذ دالة الإنترفيس دي."_ ده أخف، وأسرع، وأنضف.

##### 💡 Analogy: The Contractor 👷 (تشبيه: المقاول)

- **الطريقة القديمة (Anonymous Class):** محتاج تغير لمبة. بتقوم معين موظف بدوام كامل، وتديله يونيفورم، وكارنيه، وعقد، بس عشان يلف اللمبة. (كود كتير زيادة Boilerplate!).
- **الطريقة الجديدة (Lambda):** بتشاور على اللمبة وتقول: `() -> changeBulb()`. أنت بتركز على **الفعل (Action)**، مش العامل.

---

#### 2. The Arrow Operator (->)

**(معامل السهم)** **(Slide 117)**

##### 📖 The Surface (السطح)

المعامل الجديد ده بيقسم اللامبدا لجزئين:

1. **الجانب الأيسر:** **المعاملات/المدخلات (Parameters)**.
2. **الجانب الأيمن:** **الجسم (Body)** (الفعل/المخرجات).

```java
//    Input       Operator       Action
   (parameters)      ->       expression
```
##### 👨‍🏫 Senior Touch / Pro Tips (لمسة السينيور)

> [!TIP] **الـ Syntax مرن جداً**
> 
> - لو **مفيش** معاملات، استخدم أقواس فاضية: `() -> ...`
> - لو فيه معامل **واحد**، الأقواس اختيارية: `n -> ...`
> - لو فيه **أكتر** من معامل، الأقواس إجبارية: `(x, y) -> ...`
> - لو الجسم سطر **واحد**، الأقواس المعقوفة `{}` وكلمة `return` اختياريين.
> - لو الجسم **عدة سطور**، **لازم** تستخدم `{}` وتكتب `return` صراحة.

---

#### 3. Single Lambda Expressions (No Arguments)

**(تعبيرات لامبدا مفردة - بدون مدخلات)** **(Slide 118)**

##### 📖 The Surface (السطح)

أحياناً، الميثود مش محتاجة مدخلات بس بتنتج نتيجة (زي الـ Supplier).

- **مثال:** `() -> 98.6`
- **السلوك:** مش بياخد معاملات وبيرجع قيمة ثابتة 98.6.

**The Old Way (Verbose):**

```java
interface MyValue {
    double getValue();
}

// We had to create a whole anonymous class!
MyValue v = new MyValue() {
    public double getValue() {
        return 98.6;
    }
};
```

**The Lambda Way (Clean):**

```java
// The compiler knows MyValue has one method 'getValue()'
// that returns a double. It matches 98.6 to that return.
MyValue v = () -> 98.6;
```
##### 🧠 The Deep Dive (Lazy Evaluation) / (الغوص العميق: التقييم الكسول)

لاحظ إن لما تكتب `() -> Math.random() * 100`، الكود ده **مش بيتنفذ فوراً**. هو متغلف جوه أوبجيكت. العملية الحسابية بتحصل بس لما تنادي `.getValue()` بعدين في كودك. ده بيسمح بتحسينات أداء قوية (Lazy Evaluation).

---

#### 4. Single Lambda Expressions (One Argument)

**(تعبيرات لامبدا مفردة - مدخل واحد)** **(Slide 119)**

##### 📖 The Surface (السطح)

ماذا لو ميثود الإنترفيس محتاجة مدخل؟

- **مثال:** `(n) -> 1.0 / n`
- **السلوك:** بيرجع مقلوب الرقم n.
- **استنتاج النوع (Type Inference):** مش محتاج تحدد نوع n. المترجم بيفهم لوحده.

### 💻 Code Autopsy: `(n) -> (n % 2) == 0`

Let's look at the example from Slide 119:

```java
// Functional Interface (The Contract)
interface NumericTest {
    boolean test(int n);
}

// The Lambda (The Implementation)
// 1. 'n' is inferred to be an Integer because 'test' takes an int.
// 2. The result is boolean because 'test' returns boolean.
NumericTest isEven = (n) -> (n % 2) == 0;

// Usage
boolean result = isEven.test(10); // Returns true
```
##### 👨‍🏫 Senior Touch / Pro Tips (لمسة السينيور)

> [!WARNING] **قاعدة "النهائي فعلياً" (Effectively Final Rule)** لو استخدمت متغير من _خارج_ اللامبدا (مثلاً متغير local في الـ main method) جوه جسم اللامبدا، المتغير ده **ممنوع يتغير**.
> > ```java
> int factor = 2;
> NumericTest doubler = n -> n * factor; // OK
> // factor = 3; // ERROR! You cannot change 'factor' if it's used inside a lambda.
> ```
>  الجافا بتلتقط **قيمة** المتغير، مش المتغير نفسه.

---

### Lesson 3 (Part 3): Control Statements

**(الدرس 3 - الجزء 3: جمل التحكم)**

#### 1. Selection Statements: The Decision Makers

**(جمل الاختيار: صناع القرار)** **(Slides 121 - 130)**

##### 📖 The Surface (السطح)

الجافا بتقدم طريقتين عشان تفرع الكود بتاعك: جملة **if** وجملة **switch**.

- **Simple if:** بتنفذ الكود بس لو الشرط true.
- **if-else:** بتنفذ بلوك لو true، وبلوك تاني لو false.
- **Ladder:** سلسلة من `else if` لفحص شروط متعددة.
- **Nested:** جملة if جوه جملة if تانية.

### 💻 Code Autopsy: The `if-else-if` Ladder

Let's look at the **Season Example** from Slide 126.

```java
class IfElse {
    public static void main(String args[]) {
        int month = 4; // April
        String season;

        // The Ladder: Checks conditions top to bottom.
        // As soon as one is TRUE, the rest are skipped.
        if(month == 12 || month == 1 || month == 2)
            season = "Winter";
        else if(month == 3 || month == 4 || month == 5)
            season = "Spring";
        else if(month == 6 || month == 7 || month == 8)
            season = "Summer";
        else if(month == 9 || month == 10 || month == 11)
            season = "Autumn";
        else
            season = "Unknown Month"; // The Catch-All

        System.out.println("April is in the " + season + ".");
    }
}
```
##### 🧠 The Deep Dive: switch vs. if (الغوص العميق)

Slide 129 بتقدم الـ switch statement.

- **if-else:** بتفحص شروط منطقية (boolean). تقدر تفحص نطاقات (`x > 5`) أو منطق معقد.
- **switch:** بتفحص متغير واحد ضد **ثوابت (constants)** محددة (cases). **ما وراء الكواليس (JVM Optimization):** لما تكتب سلم if-else كبير، الـ CPU لازم يقيم كل شرط واحد ورا التاني. لما تكتب switch، مترجم الجافا (javac) غالباً بيعمل **Jump Table** أو **Lookup Switch** في البايت كود. هو مش بيفحص case 1، وبعدين case 2.. لأ، هو بيحسب عنوان الذاكرة للكيس الصح وبينط عليها علطول. التعقيد بتاعها **O(1)** (وقت ثابت)، بينما الـ if-else بتكون **O(n)**.

##### 👨‍🏫 Senior Touch / Pro Tips (لمسة السينيور)

> [!TIP] **قاعدة الـ "Break"** في جملة switch، لو نسيت تكتب `break;`، الجافا بتعمل **"Fall-through"** (سقوط حر). هتنفذ الـ case المطابقة **وكمان** كل الـ cases اللي تحتها لحد ما تخبط في break.
> 
> - _Bug:_ عايز تطبع "One"، بس طبعت "One" و "Two" و "Three".
> - _Feature:_ أحياناً إحنا _عايزين_ الـ Fall-through (مثلاً فحص لو الشهر فيه 31 يوم، بنرص Cases 1, 3, 5, 7.. فوق بعض).

---

#### 2. Iteration Statements: The Loops

**(جمل التكرار: اللوبات)** **(Slides 131 - 134)**

##### 📖 The Surface (السطح)

عندنا 3 محركات عشان نمشي اللوبات:

1. **while:** بتفحص الشرط _قبل_ التشغيل. (ممكن متشتغلش ولا مرة).
2. **do-while:** بتفحص الشرط _بعد_ التشغيل. (مضمون تشتغل مرة واحدة على الأقل).
3. **for:** لوب مدمجة. بتحتوي على التهيئة، والشرط، والتكرار في سطر واحد.
### 💻 Code Autopsy: The `for` Loop

From Slide 134:

```java
//   (Init;  Condition;  Step)
for(int i=0; i<10; i++) {
    System.out.println(i);
}
```
##### 💻 Code Autopsy: The for Loop (تشريح كود الـ for)

من Slide 134:

1. **`int i=0`:** بتشتغل مرة واحدة في البداية.
2. **`i<10`:** بتتفحص قبل كل لفة. لو false، اللوب بتموت.
3. **`System...`:** جسم اللوب بيتنفذ.
4. **`i++`:** بتشتغل _بعد_ الجسم. وبعدين نرجع لخطوة 2.

##### 🧠 The Deep Dive: Stack Overflow (الغوص العميق)

لو بوظت شرط اللوب (مثلاً `while(true)` من غير break)، هتعمل **Infinite Loop**. لو اللوب دي بتعمل Objects أو بتنادي ميثودز بشكل تكراري (Recursion)، هتوقع الـ JVM بـ `StackOverflowError` أو `OutOfMemoryError`. دايماً راجع شرط الخروج بتاعك!

---

#### 3. Jump Statements: Breaking the Flow

**(جمل القفز: كسر التدفق)** **(Slides 135 - 142)** هنا الكود بيبقى مثير للاهتمام. الجافا بتدعم `break`، `continue`، و `return`.

##### A. The break Statement (and Labels)

**(Slides 136 - 138)**

- **Standard break:** بتخرج من _أقرب_ loop أو switch حالاً.
- **Labeled break:** دي نسخة الجافا الآمنة من جملة "GOTO". بتسمحلك تكسر وتخرج من **Nested Loops** (لوبات متداخلة). 

**The Code Autopsy (Labeled Break - Slide 138):**

```java
class BreakLabelDemo {
    public static void main(String args[]) {

        // We give this loop a name: "outer"
        outer: for (int i = 0; i < 3; i++) {
            System.out.print("Pass " + i + ": ");

            for (int j = 0; j < 100; j++) {
                if (j == 10)
                    break outer; // EXITS BOTH LOOPS!

                System.out.print(j + " ");
            }
            System.out.println("This will not print");
        }
        System.out.println("Loops complete.");
    }
}
```

- **ليه نستخدمها؟** من غير `break outer`، الـ break كانت هتوقف الـ j loop بس، والـ i loop كانت هتكمل لف.

##### B. The continue Statement

**(Slides 139 - 140)**

- **الفعل:** بتوقف _اللفة الحالية_ (iteration) وبتنط فوراً للفّة اللي بعدها (خطوة التحديث update step).
- **تشبيه:** زي ما تكون بتلعب كوتشينة. سحبت ورقة وحشة؟ مش بتنسحب من اللعبة (break). أنت بس بترميها وتسحب تاني (continue).

##### C. The return Statement

**(Slide 141 - 142)**

- **الفعل:** بتنهي **الميثود** فوراً، مش بس اللوب. وبترجع للمكان اللي نادى الميثود (caller).

```java
class ReturnDemo {
    public static void main(String args[]) {
        boolean t = true;
        System.out.println("Before the return.");

        if (t) return; // The JVM leaves main() immediately.

        System.out.println("This won't execute.");
    }
}
```
##### 👨‍🏫 Senior Touch / Pro Tips (لمسة السينيور)

> [!TIP] **The "Guard Clause" Pattern** Junior developers often write "Arrow Code" (nested ifs):
> 
> ```java
> if (valid) {
>    if (authorized) {
>       // do work
>    }
> }
> ```
> 
> **Senior Developers use `return` to exit early:**
> 
> ```java
> if (!valid) return;
> if (!authorized) return;
> // do work
> ```
> 
> This is cleaner, flatter, and easier to read.


---

### Lesson 3 (Part 4): The String Architecture

**(الدرس 3 - الجزء 4: معمارية النصوص)** **(Slides 143 - 153)**

> [!ABSTRACT] **نظرة عامة على المحاضرة** هنشرح كلاس `java.lang.String`. هنتعلم:
> 
> 1. الفرق بين الـ String **Literal** والـ String **Object**.
> 2. مفهوم **Immutability** (ليه النصوص مينفعش تتغير).
> 3. **مسبح ثوابت النصوص (String Constant Pool)** (تحسين الذاكرة في الجافا).
> 4. الميثودز الأساسية وميزة **Java 7 Switch**.

#### 1. Creating Strings & The Memory Model

**(إنشاء النصوص ونمودج الذاكرة)** **(Slides 143 - 145)**

##### 📖 The Surface (السطح)

- **التعريف:** الـ String هو تسلسل من الحروف محاط بعلامات تنصيص مزدوجة `" "`.
- **طريقتين للإنشاء:**
    1. **Literal:** `String s1 = "welcome";`
    2. **New Keyword:** `String s = new String("Hello");`
- **حقيقة مهمة:** النصوص ثوابت. قيمتها لا يمكن تتغير بعد الإنشاء.

##### 🧠 The Deep Dive: The String Pool (Interning) / (الغوص العميق: مسبح النصوص)

هنا السحر بيحصل.

- **The Heap:** دي مساحة الذاكرة الكبيرة اللي بيعيش فيها كل الـ Objects.
- **The String Pool:** جوه الـ Heap، الجافا حاجزة منطقة VIP اسمها **String Constant Pool**. **سيناريو أ: الـ Literal** (`String s1 = "welcome";`)

1. الجافا بتفحص الـ Pool: "هل عندنا 'welcome' هنا؟"
2. **لأ؟** انشئيها في الـ Pool. ورجعي العنوان لـ s1.
3. **آه؟** رجعي العنوان _الموجود_. **متعمليش** أوبجيكت جديد.
    - _النتيجة:_ توفير ضخم للذاكرة لو استخدمت كلمة "welcome" ألف مرة.

**سيناريو ب: الـ new Keyword** (`String s = new String("welcome");`)

1. كلمة `new` هي أمر للـ JVM: "أنا مش مهتم بالـ Pool. اعملي **أوبجيكت جديد لانج** في الـ Heap."
2. _النتيجة:_ ممكن يكون عندك نسختين متطابقتين من "welcome" في الميموري (واحدة في الـ Pool وواحدة في الـ Heap العادي). ده يعتبر تسريب ذاكرة (إهدار).

##### 💡 Analogy: The Public Library vs. Buying Books (تشبيه: المكتبة العامة ضد شراء الكتب)

- **String Pool (Literal):** أنت بتروح المكتبة. لو كتاب "هاري بوتر" هناك، بتستعيره. مش بتطبع واحد جديد. كل الناس بتقرأ نفس النسخة.
- **new String():** أنت بتروح محل كتب وتشتري نسخة جديدة من "هاري بوتر". حتى لو عندك واحدة في البيت، بقى عندك نسختين ماديين واخدين مساحة على الرف.
### 💻 Code Autopsy

```java
String s1 = "welcome"; // Created in Pool
String s2 = "welcome"; // Reuses the SAME object from Pool

// Comparing Memory Addresses
System.out.println(s1 == s2); // TRUE! They point to the exact same bytes in RAM.

String s3 = new String("welcome"); // Forces a new object on Heap
System.out.println(s1 == s3); // FALSE! Different memory addresses.
```
---

#### 2. Immutability: The Unchangeable Object

**(عدم القابلية للتغير)** **(Slides 146 - 148)**

##### 📖 The Surface (السطح)

**Immutable** يعني "غير قابل للتغيير". بمجرد ما أوبجيكت String يتخلق، بياناته مينفعش تتعدل. لو حاولت تغيره (مثلاً `word.concat(" Technologies")`)، الجافا بتخلق String **جديد** بدل ما تغير القديم.

##### 🧠 The Deep Dive: Why Immutable? (الغوص العميق: ليه؟)

1. **الأمان (Security):** النصوص بتستخدم لأسماء المستخدمين في الداتابيز، مسارات الملفات، واتصالات الشبكة. لو مررت String `db_password` لميثود، لازم أضمن إن الميثود دي متقدرش تغيره من ورا ضهري.
2. **Thread Safety:** بما إن الداتا عمرها ما تتغير، كذا thread يقدروا يقروا نفس الـ String من غير ما يضربوا. مش محتاجين Synchronization.
3. **Performance (The Pool):** مينفعش تعمل Pool لأوبجيكتات بتتغير (mutable). لو s1 و s2 بيشاوروا على "Java"، و s1 غيرتها لـ "Python"، s2 بالغلط هتبقى "Python" هي كمان! الـ Immutability بتمنع الكارثة دي.

##### 👨‍🏫 Senior Touch / Pro Tips (لمسة السينيور)

> [!WARNING] **فخ الأداء (The Performance Trap)** Slide 148 بتبرز العيب. **إيه اللي بيحصل في الـ CPU/RAM؟**
> ```java
> String word = "Java";
> word = word.concat("Technologies");
> ```
> 1. "Java" اتخلقت.
> 2. "Technologies" اتخلقت.
> 3. "Java Technologies" (النتيجة) اتخلقت.
> 4. "Java" اتهجرت (Garbage Collection).
> 
> لو عملت ده جوه Loop بتلف 10,000 مرة، أنت بتخلق **30,000 أوبجيكت** بس عشان تبني جملة واحدة. **السيرفر بتاعك هيقع.** _الحل:_ استخدم `StringBuilder` (جاي في الدرس اللي بعده).

---

#### 3. Constructors and Methods

**(الكونستركتورز والميثودز)** **(Slides 149 - 151)**

##### 📖 The Surface (السطح)

- **Constructors:** تقدر تبني String من مصفوفة حروف `char[]`.
   ```java
    char[] letters = {'J', 'a', 'v', 'a'};
    String word = new String(letters); // "Java"
    ```
- **Methods:**
    - `length()`: بترجع عدد الحروف (لاحظ: دي ميثود `()`، مش خاصية زي المصفوفات `.length`).
    - `charAt(int i)`: بترجع الحرف عند الفهرس i.
    - `substring(int begin)`: بتقتطع جزء من النص.
    - `equals(Object o)`: بتقارن **المحتوى** (القيم).
### 💻 Code Autopsy: `substring` and `equals`

```java
String s = "helping";
// Slide 150 example
System.out.println(s.substring(2));
// Output: "lping" (Starts at index 2: 'l', and goes to the end)

String s1 = "Java";
String s2 = new String("Java");

// WRONG WAY
if (s1 == s2) { ... } // False. Compares Address.

// SENIOR WAY
if (s1.equals(s2)) { ... } // True. Compares Content ("J"="J", "a"="a"...).
```
---

#### 4. Java 7 Feature: Switch on String

**(ميزة جافا 7: Switch على النصوص)** **(Slide 152)**

##### 📖 The Surface (السطح)

قبل Java 7، جملة switch كانت بتدعم بس int, byte, short, char, و enum. كنت مضطر تستخدم سلاسل if-else قبيحة للنصوص. **Java 7+** بتسمحلك تعمل switch مباشرة على متغير String.

##### 🧠 The Deep Dive: How does it work? (الغوص العميق: بتشتغل إزاي؟)

الـ CPU مبيعرفش يقارن نصوص بكفاءة جوه switch table (هو بيحب الأرقام). لما بتعمل Compile للكود ده: 

```java
switch(mode) {
    case "ACTIVE": ...
    case "OFF": ...
}
```
مترجم الجافا (javac) بيحول الـ Strings لـ **hashCode()** بتاعهم (تمثيل رقمي). وبيعمل switch بناءً على أرقام الـ hash دي.

- **فحص أمان:** عشان ممكن نصين مختلفين يكون ليهم نفس الـ hash (تصادم Collision)، الجافا بتضيف فحص `equals()` نهائي جوه الـ case المطابقة عشان تبقى متأكدة 100%.

### 💻 Senior Code Example

```java
String status = "ERROR";

switch (status) {
    case "OK":
        System.out.println("System Healthy");
        break;
    case "WARNING":
        System.out.println("Check logs");
        break;
    case "ERROR":
        System.out.println("System Failure!");
        break;
    default:
        System.out.println("Unknown Status");
}
```

---

### Lesson 3 (Part 4): Mutable Strings & Tokenizing

**(الدرس 3 - الجزء 4 تكملة: النصوص القابلة للتغيير والتقطيع)**

> [!ABSTRACT] **نظرة عامة على المحاضرة** هنتعلم إزاي نتلاعب بالنصوص بكفاءة من غير ما ندمر الميموري.
> 
> - **StringBuffer:** معالج النصوص الآمن (Thread-safe) والقابل للتغيير.
> - **StringBuilder:** معالج النصوص عالي الأداء (غير آمن للـ Threads).
> - **StringTokenizer:** الطريقة الكلاسيكية لتقطيع الجمل لكلمات.

**الشرائح:** 155 – 161

#### 1. The Mutable Saviors: StringBuffer & StringBuilder

**(المنقذون المتغيرون)** **(Slides 155 - 156)**

##### 📖 The Surface (السطح)

- **المشكلة:** الـ String ثابت (Immutable). تعديله بيخلق زبالة (Garbage).
- **الحل:** الجافا بتقدم StringBuffer و StringBuilder. دول كلاسات **Mutable**. تقدر تعدل النص _جوه_ نفس مكان الذاكرة.

##### 🧠 The Deep Dive: Synchronized vs. Non-Synchronized (الغوص العميق)

Slide 155 & 156 بيوضحوا الفرق الجوهري:

- **StringBuffer:** هو **Synchronized**. وده معناه إنه **Thread-Safe**. لو 2 threads حاولوا يضيفوا نص لنفس الـ buffer في نفس الوقت، الجافا بتوقفهم طابور. الأمان ده بيجي بتكلفة: هو **أبطأ**.
- **StringBuilder:** هو **Not Synchronized**. هو **Not Thread-Safe**. لكن، عشان هو مش بيضيع وقت في فحص "الأقفال" (locks)، هو **أسرع** بفرق ملحوظ.

##### 💡 Analogy: The Public Diary 📔 (تشبيه: المذكرات العامة)

- **String (Immutable):** كتاب مطبوع. عشان تغير كلمة، لازم تعيد طباعة الكتاب كله.
- **StringBuffer (Synchronized):** مذكرات واحدة في أوضة مقفولة. شخص واحد بس معاه المفتاح. لو عايز تكتب، لازم تستنى اللي قبلك يخرج ويديك المفتاح. (آمن، بس بطيء).
- **StringBuilder (Not Synchronized):** ورقة على ترابيزة مشتركة. كل الناس بتمسك أقلام وتكتب في نفس الوقت. سريعة، بس لو اتنين كتبوا في نفس الحتة، النص هيبوظ (Corrupted Data).

##### 👨‍🏫 Senior Touch / Pro Tips (لمسة السينيور)

> [!TIP] **امتى نستخدم إيه؟**
> 
> - **99% من الوقت:** استخدم **StringBuilder**. ليه؟ لأن معظم المتغيرات اللي بتعملها جوه ميثود (local variables) مبيشوفهاش غير thread _واحد_ (اللي هو الحالي). مش محتاج تكلفة الـ synchronization.
> - **1% من الوقت:** استخدم **StringBuffer** بس لو بتشارك متغير نصي global بين threads متعددة.

---

#### 2. Constructors & Memory Management

**(الكونستركتورز وإدارة الذاكرة)** **(Slide 157)**

##### 📖 The Surface (السطح)

تقدر تنشئ StringBuffer (أو Builder) بـ 3 طرق:

1. `new StringBuffer()`: بيعمل buffer فاضي بسعة افتراضية (عادةً 16 حرف).
2. `new StringBuffer(int capacity)`: بيعمل buffer فاضي بحجم محدد.
3. `new StringBuffer(String s)`: بيعمل buffer جواه النص s.

##### 🧠 The Deep Dive: Capacity vs. Length (الغوص العميق)

- **Length:** عدد الحروف المتخزنة حالياً (مثلاً "Hi" = 2).
- **Capacity:** كمية الذاكرة المحجوزة فعلياً (مثلاً 16).
- **النمو التلقائي (Auto-Growth):** لو ضفت نص أكبر من السعة، الجافا بتوسع المصفوفة أوتوماتيكياً. عادةً بتعمل مصفوفة جديدة بحجم `(oldCapacity * 2) + 2`، وتنسخ الداتا القديمة، وترمي المصفوفة القديمة.
    - **نصيحة تحسين:** لو عارف إنك بتبني نص ضخم (مثلاً ملف XML حجمه 1MB)، حدد السعة في الكونستركتور: `new StringBuilder(1000000)`. ده بيمنع الجافا من إنها تعمل resizing للمصفوفة مرات ورا مرات.

---

#### 3. The Methods (The Toolkit)

**(الأدوات / الميثودز)** **(Slide 158)**

|الميثود|الوصف|مثال|
|:--|:--|:--|
|**append(x)**|بيضيف نص في _الآخر_.|`sb.append(" World");`|
|**insert(i, x)**|بيحشر نص عند الفهرس i.|`sb.insert(0, "Hello ");`|
|**delete(start, end)**|بيمسح نص.|`sb.delete(2, 4);`|
|**reverse()**|بيشقلب النص.|`sb.reverse();` (abc -> cba)|
|**replace(start, end, str)**|بيبدل حتة من النص.|`sb.replace(0, 1, "H");`|

---

#### 4. StringBuilder: The Speed Demon

**(شيطان السرعة)** **(Slide 159)**

##### 📖 The Surface (السطح)

- نفس الميثودز بتاعة StringBuffer.
- نفس الكونستركتورز.
- **الفرق:** مش synchronized.
- **الفائدة:** حمل (overhead) أقل، أسرع في البيئات أحادية الـ thread.

---

#### 5. StringTokenizer (The Legacy Chopper)

**(المقطع القديم)** **(Slides 160 - 161)**

##### 📖 The Surface (السطح)

- **الغرض:** تكسير نص لقطع (tokens) بناءً على **فواصل** (delimiters زي المسافة، الفاصلة، إلخ).
- **الباقة:** `java.util`.
- **الاستخدام:** مفيد لعمل Parsing لملفات داتا بسيطة (CSV) أو جمل.

##### 🧠 The Deep Dive: How it works (الغوص العميق)

بيشتغل زي مؤشر (cursor) بيتحرك جوه النص.

- `hasMoreTokens()`: بتفحص هل لسه فيه قطع باقية.
- `nextToken()`: بترجع القطعة اللي عليها الدور وبتحرك المؤشر لقدام.
### 💻 Code Autopsy: `Tokenizer` vs `Split`

**The `StringTokenizer` Way (Slide 161 Logic):**

```java
import java.util.StringTokenizer;

String line = "Java,Python,C++";
// delim: "," means split by comma
StringTokenizer st = new StringTokenizer(line, ",");

while (st.hasMoreTokens()) {
    String token = st.nextToken();
    System.out.println(token);
}
```

**The Modern `String.split()` Way:**

```java
String[] tokens = line.split(",");
for (String token : tokens) {
    System.out.println(token);
}
```
##### 👨‍🏫 Senior Touch / Pro Tips (لمسة السينيور)

> [!WARNING] **تنبيه تراثي (Legacy Alert)** الـ `StringTokenizer` يعتبر "Legacy Class".
> 
> - **للتقطيع البسيط:** استخدم `String.split()`. بتستخدم Regular Expressions (Regex) وأقوى بكتير.
> - **امتى تستخدم Tokenizer؟** تقنياً، StringTokenizer أسرع من split() لأنه _مش_ بيستخدم Regex. لو بتعمل Parsing لملف نصي ضخم (جيجابايتس) بفاصل بسيط (زي المسافة)، الـ StringTokenizer ممكن يوفرلك شوية دورات CPU. غير كده، خليك في split().

---

### Lab Exercise: String Manipulation Mastery

**(تمرين المعمل: إتقان التلاعب بالنصوص)** **(Slide 162)**

> [!NOTE] **بيانات المهمة (The Mission Data)** **الجملة المدخلة:** "ITI develops people and ITI house of developers and ITI for people" **الكلمة المستهدفة:** "ITI" **الأهداف:**
> 
> 1. عد مرات التكرار باستخدام ميثودز String (طريقتين مختلفتين).
> 2. عد مرات التكرار باستخدام StringTokenizer.
> 3. قطع نص عنوان IP (الفخ الخفي!).

#### Part 1: Using String Methods (2 Ways)

**(الجزء 1: باستخدام ميثودز String)**

##### Way 1: The "Split and Iterate" Method (The Modern Way)

دي الطريقة الأكثر قراءة. بنقطع الجملة لمصفوفة كلمات ونفحصهم واحدة واحدة.
#### 💻 Code Autopsy

```java
public class WordCountSplit {
    public static void main(String[] args) {
        String text = "ITI develops people and ITI house of developers and ITI for people";
        String target = "ITI";
        int count = 0;

        // 1. Split the string by space
        // This creates an array: ["ITI", "develops", "people", ...]
        String[] words = text.split(" ");

        // 2. Iterate (Enhanced For-Loop)
        for (String word : words) {
            // 3. Compare Content (Never use ==)
            if (word.equals(target)) {
                count++;
            }
        }
        System.out.println("Method 1 (Split): Found " + count + " times.");
    }
}
```
##### Way 2: The "IndexOf" Method (The Performance Way)

ماذا لو النص حجمه 1 جيجا؟ إنشاء مصفوفة `String[]` (طريقة 1) ممكن يكرش الميموري بتاعتك. ميثود `indexOf` بتبحث جوه النص مباشرة من غير ما تعمل مصفوفات زيادة. هي بتنط من تكرار للتاني.
#### 💻 Code Autopsy

```java
public class WordCountIndex {
    public static void main(String[] args) {
        String text = "ITI develops people and ITI house of developers and ITI for people";
        String target = "ITI";
        int count = 0;
        int index = 0;

        // 1. Loop while indexOf finds the word
        // text.indexOf(target, index) searches for 'target' starting from 'index'
        while ((index = text.indexOf(target, index)) != -1) {

            count++; // Found one!

            // 2. Move the cursor forward so we don't count the same one again.
            // We jump forward by the length of the word.
            index += target.length();
        }

        System.out.println("Method 2 (IndexOf): Found " + count + " times.");
    }
}
```
##### 🧠 The Deep Dive: Memory vs. Readability (الذاكرة ضد القراءة)

- **طريقة 1 (Split):** بتنشئ أوبجيكت String جديد لكل كلمة _واحدة_ في الجملة. استهلاك عالي للذاكرة. كويسة للنصوص الصغيرة.
- **طريقة 2 (IndexOf):** بتنشئ تقريباً صفر زبالة (Garbage). هي بس بتحرك مؤشر رقم صحيح (index) جوه النص الموجود. **أسرع بكتير** للملفات الضخمة.

#### Part 2: Using StringTokenizer

**(الجزء 2: استخدام StringTokenizer - الطريقة القديمة)** دي بتستخدم نهج المؤشر اللي ناقشناه. كفؤة بس مميزاتها أقل من الـ Regex.
#### 💻 Code Autopsy

```java
import java.util.StringTokenizer;

public class WordCountToken {
    public static void main(String[] args) {
        String text = "ITI develops people and ITI house of developers and ITI for people";
        String target = "ITI";
        int count = 0;

        // 1. Create Tokenizer (default delimiter is whitespace)
        StringTokenizer st = new StringTokenizer(text);

        // 2. Iterate while tokens exist
        while (st.hasMoreTokens()) {
            String word = st.nextToken();
            if (word.equals(target)) {
                count++;
            }
        }
        System.out.println("Method 3 (Tokenizer): Found " + count + " times.");
    }
}
```
#### Part 3: Splitting an IP Address (The Trap!) 🚨

**(الجزء 3: تقطيع عنوان IP - الفخ!)** المعمل بيطلب: _"قطع نص بيمثل IP Address."_ مثال: 192.168.1.1. لو جربت `str.split(".")`، البرنامج هيفشل. **ليه؟**

##### 🧠 The Deep Dive: Regex vs. Literal (الغوص العميق)

ميثود `split()` بتاخد **Regular Expression** (Regex)، مش مجرد نص عادي.

- في الـ Regex، النقطة `.` هي wildcard معناها **"أي حرف"**.
- فـ `split(".")` بتقول للجافا: "قطعي النص ده عند كل حرف."
- **النتيجة:** مصفوفة فاضية.

عشان نصلح ده، لازم نعمل **Escape** للنقطة باستخدام backslashes `\\`. **الكود الصح:** `str.split("\\.")`
#### 💻 Code Autopsy

```java
public class IPSplitter {
    public static void main(String[] args) {
        String ip = "192.168.1.1";

        // WRONG:
        // String[] parts = ip.split("."); // Returns empty array!

        // CORRECT:
        // We use "\\" to tell Java "Treat the next character as a real dot"
        String[] parts = ip.split("\\.");

        System.out.println("Splitting IP: " + ip);
        for (String part : parts) {
            System.out.println(part);
        }
    }
}
```

**Output:**

```java
192
168
1
1
```