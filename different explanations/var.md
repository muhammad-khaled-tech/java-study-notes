الـ `var` دي حكاية كبيرة ظهرت في **Java 10** تحت اسم **Local Variable Type Inference**. وعشان تضمن أي سؤال يجيلك فيها في الامتحان، لازم تفهم هي "إيه" وهي "مش إيه".

إليك الشرح التفصيلي الممل (زي ما الكتاب بيقول):

---

### 1. يعني إيه `var`؟ (The Concept)

الـ `var` مش نوع جديد (Data Type) زي `int` أو `String`. هي مجرد **تعليمة للكومبايلر** بتقوله:

> *"بص على القيمة اللي بعد علامة يساوي `=`، واستنتج أنت النوع لوحدك."*

**مثال:**

```java
var x = "Hello";
// الكومبايلر بيشوف "Hello" (String)، فبيحول الكود لـ:
// String x = "Hello";

```

⚠️ **مهم جداً:** الاستنتاج ده بيحصل وقت الـ **Compilation** (مش وقت الـ Runtime). يعني بمجرد ما الكومبايلر حدد النوع، الموضوع انتهى والنوع بقى ثابت.

---

### 2. القوانين الصارمة (Rules & Restrictions) 🚨

عشان تستخدم `var`، فيه شروط لازم تتحقق، وإلا تاخد **Compilation Error**:

#### أ) لازم يكون فيه قيمة ابتدائية (Initialization)

الكومبايلر غلبان، محتاج حاجة يستنتج منها.

```java
var x = 10;   // ✅ صح: النوع int
var y;        // ❌ خطأ: الكومبايلر مش عارف النوع إيه.

```

#### ب) ممنوع مع الـ `null` الصريح

زي ما شفنا في السؤال اللي فات، `null` ملهاش نوع محدد.

```java
var z = null; // ❌ خطأ
var z = (String) null; // ✅ صح (لأنك عملت Casting)

```

#### ج) مكانها فين؟ (Scope)

الـ `var` بتشتغل فقط مع **Local Variables** (المتغيرات اللي جوه الميثود أو اللوب).

* ❌ **ممنوع:** تكون Instance Variable (متغير في الكلاس).
* ❌ **ممنوع:** تكون Parameter في ميثود (`void test(var x)`).
* ❌ **ممنوع:** تكون Return Type لميثود (`public var get()`).

#### د) الثبات (Immutability of Type)

بمجرد ما الكومبايلر حدد النوع في السطر الأول، مينفعش تغيره.

```java
var number = 10; // الكومبايلر قرر إن ده int
number = "Text"; // ❌ Compilation Error: incompatible types

```

---

### 3. التكات الخادعة في الامتحان (Exam Traps) 🔥

#### التكة 1: المصفوفات (Arrays)

أنت متعود تكتب المصفوفة كده: `int[] arr = {1, 2, 3};`.
مع `var`، الطريقة المختصرة دي **ممنوعة**.

```java
var arr = {1, 2, 3}; // ❌ خطأ: الكومبايلر مش عارف دي مصفوفة int ولا long ولا byte
var arr = new int[]{1, 2, 3}; // ✅ صح: لازم تحدد النوع صراحة

```

#### التكة 2: الوراثة والبوليمرفيزم (Polymorphism)

دي أخطر حتة.

```java
var list = new ArrayList<String>();

```

هنا `list` نوعها إيه؟

* هل هي `List<String>`؟ (Parent)
* ولا `ArrayList<String>`؟ (Child)

**الإجابة:** الكومبايلر بياخد النوع المحدد بالظبط، يعني `ArrayList<String>`.
لو كنت عايزها `List`، لازم تكتبها بالطريقة القديمة أو تعمل Casting.

#### التكة 3: الـ Lambdas

الكومبايلر مقدرش يستنتج نوع الـ Lambda لوحدها.

```java
var f = () -> System.out.println("Hi"); // ❌ خطأ
Runnable f = () -> System.out.println("Hi"); // ✅ صح

```

---

### 4. معلومة "للمحترفين" (Pro Tip)

كلمة `var` ليست **Reserved Keyword** (زي `int` أو `class`)، هي **Reserved Type Name**.
يعني إيه؟
يعني ينفع تسمي متغير باسم `var`! 😲

```java
int var = 5; // ✅ شغال وزي الفل
var var = 10; // ✅ شغال (الأولى type والثانية اسم المتغير)

```

لكن **مينفعش** تسمي كلاس باسم `var`:

```java
class var {} // ❌ Compilation Error

```

### الخلاصة (The Summary map):

1. **المكان:** جوه الميثود بس (Local).
2. **الشرط:** لازم قيمة (Initializer) ومش `null`.
3. **النوع:** ثابت بعد أول سطر (مبيتغيرش).
4. **الوقت:** بيتحسم وقت الـ Compilation.