

### التمهيد: حدوتة "الريموت ذو الزر الواحد" 🔘

#### س: تخيل لو عندك ريموت كنترول فيه زرار واحد بس لونه أحمر.. هتحتار؟

ج:

أكيد لأ.

- لو الريموت فيه 100 زرار، لازم أقولك: "دوس على الزرار الخامس من اليمين اللي فوق".
    
- لكن لو الريموت فيه **زرار واحد بس**، أنا مش محتاج أشرحلك. أنا هرميلك الريموت وأقولك: "شغل!". أنت أوتوماتيك هتدوس على الزرار الوحيد الموجود.
    

هو ده بالظبط الـ Functional Interface.

هو إنترفيس قرر إنه يكون "متخصص جداً"، ومفيهوش غير دالة واحدة يتيمة (Abstract Method).

- **ليه؟** عشان لما الجافا تشوفه، متحتارش. تبقى عارفة إنك لما تبعت كود، الكود ده مكانه في الدالة الوحيدة دي. وده اللي سمح لنا نكتب كود مختصر جداً (Lambda) من غير ما نكتب اسم الدالة.
    

---

### الربط: من الزرار للـ Functional Interface 🔗

في السلايد اللي فات، ActionListener كان فيه دالة واحدة actionPerformed. ده كان "ريموت بزرار واحد". عشان كده قدرنا نختصره.

السلايد ده بيحط "القانون" الرسمي للموضوع ده.

تعالى نفصص السلايد بنظام الأسئلة والأجوبة الدسمة:

---

### س1: يعني إيه Functional Programming أصلاً؟ (فلسفة الـ What vs How)

ج:

البرمجة التقليدية (Imperative) عاملة زي ما تكون بتدي وصفة طبخ لواحد مبتدأ:

1. هات بيض.
    
2. اكسر البيض.
    
3. حطه في الطاسة.
    
4. قلب لمدة دقيقتين.
    

- **أنت بتركز على "How" (ازاي تعمل الحاجة خطوة خطوة).**
    

البرمجة الوظيفية (Functional) عاملة زي ما تكون في مطعم:

- "أنا عايز بيض مقلي".
    
- **أنت بتركز على "What" (أنت عايز إيه)، ومش شاغل بالك الطباخ هيعمله ازاي.**
    

في الجافا، الـ **Functional Interface** هو "الطبق" اللي بنقدم فيه الأوامر دي.

---

### س2: ما هو الشرط الوحيد عشان نقول على Interface إنه "Functional"؟

ج:

الشرط موجود في الاسم التاني ليه: SAM.

Single Abstract Method.

يعني إنترفيس يحتوي على دالة واحدة فقط (Abstract).

- ✅ `Runnable` (فيه `run` بس) -> ينفع Functional.
    
- ✅ `ActionListener` (فيه `actionPerformed` بس) -> ينفع Functional.
    
- ❌ `MouseListener` (فيه 5 دوال للكليك والتحريك..) -> **ماينفعش** Functional (لأن الريموت فيه 5 زراير، الجافا مش هتعرف تختار مين فيهم).
    

---

### س3: إيه لازمة الـ Annotation اللي اسمه `@FunctionalInterface`؟

ج:

دي "يافطة تحذير" للمبرمجين اللي معاك (وللـ Compiler).

تخيل إنك عملت إنترفيس وتعبت فيه عشان يكون Functional (زرار واحد). جه واحد صاحبك بعد شهر، قام مزود دالة تانية في الإنترفيس بالغلط.

- **النتيجة:** كل كود الـ Lambda في المشروع هيضرب! 💥
    

الحل:

بتحط @FunctionalInterface فوق الإنترفيس.

- معناها: "يا كومبايلر، خلي بالك، الإنترفيس ده لازم يفضل بدالة واحدة. لو حد حاول يزود دالة تانية، **اطلعله Error ومترداش تعمل Compile**".
    

---

### س4: تحليل "المشكلة" في كود السلايد (ليه طلع Error؟)

تعالى نشوف الكود اللي في السلايد ونشوف ليه الكمبيوتر زعل:



```Java
@FunctionalInterface // 1. أنت حلفت للكمبيوتر إن ده زرار واحد
public interface MyFirstFunctionalInterface {
    
    public void firstWork(); // الدالة الأولى (تمام)
    
    public void doSomeMoreWork(); // 2. الدالة الثانية (كارثة!)
}
```

**التحليل:**

1. أنت حطيت اليافطة `@FunctionalInterface`.
    
2. لكن جوه الكلاس، حطيت دالتين (`firstWork` و `doSomeMoreWork`).
    
3. الكومبايلر شاف تناقض: "أنت بتقول إنه زرار واحد، وحاطط لي زرارين؟".
    
4. **النتيجة:** `Compilation Error: multiple non-overriding abstract methods found`.
    

---

### دراسة حالة: التطور التاريخي (المشكلة والحل) 🏛️

عشان تقدر قيمة الموضوع ده، لازم تشوف "القرف" اللي كنا فيه زمان.

المشكلة القديمة (The Boilerplate Nightmare):

زمان، لو عايز أعمل Thread يطبع كلمة، كنت بضطر أعمل Anonymous Inner Class (كلاس ملوش اسم) عشان أنفذ إنترفيس Runnable.



```Java
// الطريقة القديمة (قبل Java 8)
// زحمة، أقواس كتير، وكود ملوش لازمة عشان سطر واحد
Thread t = new Thread(new Runnable() {
    @Override
    public void run() {
        System.out.println("Hello from Old Java!");
    }
});
```

- **العيب:** أنا بكتب 5 سطور عشان أنفذ سطر واحد! أنا مضطر أقول للجافا "شغلي دالة run" مع إن `Runnable` مفيهوش غيرها أصلاً!
    

الحل الجديد (Lambda + Functional Interface):

بما إن Runnable هو Functional Interface (زرار واحد)، الجافا بتقولك: "متقوليش اسم الدالة، أنا عارفها. اديني الكود علطول".

Java

```Java
// الطريقة الحديثة (Java 8+)
// لا فيه new ولا فيه Override ولا اسم الدالة
Thread t = new Thread(() -> System.out.println("Hello from Modern Java!"));
```

### الخلاصة (الزتونة) 🫒

1. **Functional Interface** = إنترفيس فيه دالة `Abstract` واحدة فقط (SAM).
    
2. **@FunctionalInterface** = حماية (Validation) عشان تضمن إنك متكسرش القاعدة دي بالغلط.
    
3. **الهدف** = السماح باستخدام الـ **Lambda Expressions** (كتابة الكود باختصار شديد والتركيز على الـ "What" مش الـ "How").
    

---
بما إننا فهمنا "القانون" (SAM - Single Abstract Method)، تعالى نشوف **أمثلة عملية** بتغطي كل الحالات (السهلة، والخادعة، والممنوعة).

هنقسم الأمثلة دي بنظامنا المفضل: **سؤال، وكود، وتحليل**.

---

### التمهيد: قاعدة "الكرسي الفاضي" 🪑

تخيل إن "الإنترفيس" ده طاولة اجتماعات.

- **Functional Interface** معناه إن فيه **كرسي واحد بس فاضي** (Abstract Method).
    
- الباقي ممكن يكونوا كراسي مليانة ناس (Default/Static Methods)، دول ميهموناش.
    
- المهم إنك لما تدخل الغرفة (Lambda)، ميكونش قدامك غير كرسي واحد بس تقعد عليه عشان متبقاش محتار.
    

---

### س1: المثال المثالي (السهل الممتنع)

**السؤال:** إيه هو أبسط شكل للـ Functional Interface؟

**الكود:**

Java

```
@FunctionalInterface
public interface SimplePrinter {
    // 1. كرسي واحد فاضي (Abstract Method)
    void print(String message);
}
```

**التحليل:**

- ده إنترفيس سليم 100%.
    
- فيه دالة واحدة ناقصة `print`.
    
- الـ Lambda هتبقى: `msg -> System.out.println(msg)`
    

---

### س2: المثال المخادع (هل الـ Default بيكسر القاعدة؟) 🤔

**السؤال:** لو ضفت دوال `default` أو `static`.. هل الإنترفيس بيبوظ وميبقاش Functional؟

**الكود:**



```Java
@FunctionalInterface
public interface SmartCalculator {
    
    // 1. الدالة الـ Abstract الوحيدة (الكرسي الفاضي)
    int calculate(int a, int b);


    // 2. دالة Default (كرسي مليان - ليها كود)
    default void printDescription() {
        System.out.println("I am a calculator");
    }

    // 3. دالة Static (يافطة - ليها كود)
    static boolean isZero(int n) {
        return n == 0;
    }
}
```

**التحليل:**

- **الإنترفيس ده سليم 100% (Functional)!** ✅
    
- **السبب:** القاعدة بتقول "Single **Abstract** Method".
    
- الدوال الـ `default` والـ `static` ليهم Body (مش Abstract)، فمش بيتحسبوا معانا في العدد. الـ Lambda هتروح تنفذ `calculate` بس وتطنش الباقي.
    

---

### س3: المثال المستحيل (ليه ده Error؟) ❌

**السؤال:** إيه اللي يحصل لو حطيت دالتين Abstract؟

**الكود:**

Java

```Java
@FunctionalInterface
public interface InvalidInterface {
    
    void doWork();   // الكرسي الفاضي الأول
    
    void doMore();   // الكرسي الفاضي الثاني (المشكلة هنا)
}
```

**التحليل:**

- **Compilation Error:** `Unexpected @FunctionalInterface annotation`.
    
- **السبب:** الـ Lambda مش هتعرف تروح لمين فيهم. الريموت فيه زرارين، ومينفعش أدوس عليهم بضغطة واحدة.
    

---

### س4: دراسة حالة (Old vs New) - تطبيق عملي 🛠️

تعال نطبق الكلام ده على مثال `SmartCalculator` اللي فوق، ونشوف الفرق بين "العذاب القديم" و "الراحة الجديدة".

المشكلة القديمة (Anonymous Inner Class):

عشان أعمل عملية جمع، كنت لازم أكتب كود "رسمي" بزيادة:

Java

```Java
// لازم أعمل "new" واكتب اسم الدالة بايدي
SmartCalculator adder = new SmartCalculator() {
    @Override
    public int calculate(int a, int b) {
        return a + b;
    }
};

System.out.println("Result: " + adder.calculate(5, 10));
```

الحل الجديد (Lambda Expression):

بما إنه Functional Interface (كرسي واحد)، الجافا بتقولك "ادخل في الموضوع علطول":

Java

```Java
// يا جافا، خدي الرقمين دول (a, b) واجمعيهم (a + b)
// الجافا فهمت لوحدها إن الكود ده بتاع دالة 'calculate'
SmartCalculator adder = (a, b) -> a + b;

System.out.println("Result: " + adder.calculate(5, 10));
```

---

### الخلاصة (الزتونة) 🫒

1. **المسموح:** دالة Abstract واحدة فقط.
    
2. **الاستثناءات:** عدد لا نهائي من الـ `default` و `static` methods (لأن ليهم كود).
    
3. **استثناء للمحترفين:** دوال الـ `Object` class (زي `toString` و `equals`) لو كتبتها abstract جوه الإنترفيس مش بتتحسب، لأن كل الكلاسات بتورث تنفيذها من `Object` غصب عنها.
    

جاهز ندخل على السلايد اللي بعده (غالباً هيشرح أنواع الـ Built-in Functional Interfaces الجاهزة زي `Predicate` و `Consumer`)؟ 🚀

---
Generic interface:


إحنا بنتكلم عن السطر ده:



```Java
@FunctionalInterface
public interface SomeTest<T> {
    boolean test(T n, T m);
}
```

هنقسم الشرح بنظام **"سؤال وجواب"** زي ما اتفقنا:

---

### سؤال: مما يتكون الـ Declaration للـ Generic Functional Interface؟

#### س1: يعني إيه `<T>` اللي لازقة في اسم الإنترفيس فوق؟

ج:

دي "فتحة الصندوق" 📦.

- `public interface SomeTest` : ده اسم الإنترفيس عادي.
    
- `<T>` : هنا أنت بتقول للجافا: "يا جماعة الإنترفيس ده **مش كامل**. الإنترفيس ده ناقصه **نوع (Type)** لازم اللي هيستخدمه هو اللي يحدده".
    
- حرف الـ **T** ده مجرد رمز (اختصار لـ Type). ممكن تسميه `<X>` أو `<Data>` أو أي حاجة، بس العرف جرى إننا نسميه `T`.
    

---

#### س2: ليه كتبنا `T` جوه الأقواس `(T n, T m)`؟

ج:

دي "شروط الدخول".

- الدالة `test` بتقول: "أنا باخد اتنين Parameters".
    
- بس نوعهم إيه؟ `int`؟ `String`؟
    
- هي بتقول: "أنا نوعي نفس نوع الـ `T` اللي اتكتب فوق".
    
- **الربط السحري:** لو المستخدم قال `SomeTest<String>`، الجافا أوتوماتيك هتشيل كل `T` جوه الدالة وتحط مكانها `String`.
    

---

#### س3: تعالى نقرأ السطر "ترجمة حرفية" عشان يثبت

تعالى نترجم الكود ده للغة بشرية:

Java

```Java
public interface SomeTest<T>
```

> **الترجمة:** "أنا إنترفيس اسمه `SomeTest`. أنا مرن، وبقبل نوع مجهول هنسميه مؤقتاً `T`."

Java

```Java
boolean test(T n, T m);
```

> **الترجمة:** "عندي دالة وحيدة اسمها `test`. وظيفتها تاخد حاجتين (`n` و `m`) بس بشرط: لازم الحاجتين دول يكونوا من نفس النوع المجهول `T` اللي اتفقنا عليه فوق. وفي الآخر هرجعلك إجابة نعم أو لا (`boolean`)."

---

#### س4: إيه اللي بيحصل في "الكواليس" لما أجي استخدمه؟

عشان تفهم الـ Declaration، لازم تشوف نتيجته.

**1. لو قلت:** `SomeTest<Integer>` الكومبايلر في خياله بيشطب الـ `T` ويخلي الـ Declaration كأنه كده:

Java

```Java
public interface SomeTest {
    boolean test(Integer n, Integer m); // T بقت Integer
}
```

**2. لو قلت:** `SomeTest<String>` الكومبايلر بيحوله في خياله لكده:



```Java
public interface SomeTest {
    boolean test(String n, String m); // T بقت String
}
```

### الخلاصة (الزتونة) 🫒

الـ Declaration ده عبارة عن **"قالب" (Template)**

---

### سؤال: يعني إيه Generic Functional Interface وليه هو "الجوكر"؟

#### س1: إيه المشكلة لو معنديش Generics؟ (مشكلة التكرار)

ج:

تخيل لو عايز تعمل اختبار (Test) لرقمين تشوف بيقبلوا القسمة ولا لأ. هتعمل:

interface IntegerTest { boolean test(int a, int b); }

طب لو جالك طلب تاني، عايز تختبر نصين (Strings) وتشوف مين أطول؟ هتعمل:

interface StringTest { boolean test(String a, String b); }

- **المشكلة:** هتلاقي نفسك بتعمل 100 إنترفيس، كلهم فيهم دالة واحدة بتاخد اتنين وترجع `boolean`. تكرار ممل!
    

---

#### س2: إزاي الـ Generic `<T>` حل المشكلة دي؟ (الحل السحري)

ج:

الجافا قالتلك: "بدل ما تحدد النوع (int أو String)، حط مكانه رمز (زي T)".

- الـ `T` ده عامل زي "صندوق فاضي".
    
- وقت الاستخدام، أنت اللي بتقرر تحط جوه الصندوق ده إيه (Integer, String, Employee, ...).
    

الإنترفيس اللي في السلايد `SomeTest<T>` هو ده "الجوكر". ينفع يشتغل مع أي نوع بيانات في الدنيا.

---

#### س3: تحليل كود السلايد (الطريقة القديمة - Hard Way) 👴

السلايد جايب مثال بيستخدم الطريقة "التقليدية" عشان ينفذ الجينيرك إنترفيس.

**الكود:**



```Java
// هنا أنت جيت تنفذ، فقررت تقفل الصندوق على نوع Integer
class SomeTestImpl implements SomeTest<Integer> { 
    public boolean test(Integer n, Integer m) {
        return n % m == 0;
    }
}
```

- **التحليل:** أنت عملت كلاس مخصوص `SomeTestImpl` وظيفته الوحيدة إنه يختبر الأرقام. أنت هنا قتلت ميزة الـ Generics شوية لأنك ربطت الكلاس ده بـ `Integer` للأبد.
    

---

#### س4: إزاي نعمل الكلام ده بـ Lambda وناخد قوة الـ Generics بجد؟ (The Modern Way) 🚀

هنا تظهر عظمة Java 8. إحنا مش محتاجين نعمل كلاس `SomeTestImpl`. إحنا ممكن نستخدم الإنترفيس ده مع أنواع مختلفة في نفس اللحظة!

**المثال الحديث (دراسة حالة):**

Java

```Java
public class GenericLambdaDemo {
    public static void main(String[] args) {
        
        // 1. استخدام الجوكر مع الأرقام (Integer)
        // الـ T هنا بقت Integer
        SomeTest<Integer> isFactor = (n, m) -> (n % m) == 0;
        
        if(isFactor.test(10, 2)) 
            System.out.println("2 is a factor of 10");


        // 2. استخدام نفس الجوكر مع النصوص (String) 🔥
        // الـ T هنا بقت String من غير ما أعمل إنترفيس جديد!
        // الاختبار: هل النص الأول يحتوي على النص الثاني؟
        SomeTest<String> stringContains = (str, part) -> str.contains(part);

        if(stringContains.test("Mohamed ITI", "ITI"))
            System.out.println("Yes, ITI is found!");
    }
}
```

---

### س5: هل الجافا فيها إنترفيس جاهز بيعمل نفس الوظيفة دي؟ (معلومة للمحترفين)

**ج:** أيوة! الإنترفيس اللي في السلايد `SomeTest<T>` (بياخد اتنين ويرجع `boolean`) هو هو بالظبط إنترفيس مشهور جداً في Java 8 اسمه: **`BiPredicate<T, U>`** (Bi = ثنائي، Predicate = شرط/اختبار).

يعني في الشغل الحقيقي، إحنا مش بنكتب `SomeTest`، بنستخدم `BiPredicate` الجاهز علطول.

---

### الخلاصة (الزتونة) 🫒

1. **Generic Functional Interface:** هو إنترفيس "مرن" بيستخدم `<T>` عشان يشتغل مع أي نوع بيانات.
    
2. **الفائدة:** بتكتب الإنترفيس مرة واحدة، وتستخدمه مع `Integer`، `String`، `Car`، أي حاجة.
    
3. **التطبيق:** بدل ما تعمل كلاس `Impl` لكل نوع، استخدم **Lambda** عشان تحدد اللوجيك والنوع في سطر واحد وقت الاستخدام.
---


### سؤال: ما هي الـ Built-in Functional Interfaces وكيف نختار المناسب منها؟

### 1. الـ Predicate (المفتش) 👮‍♂️

**س1: إمتى أستخدم `Predicate<T>`؟** **ج:** لما تكون عايز **تختبر** حاجة وترجع `true` أو `false`.

- **الدالة:** `boolean test(T t)`
    
- **التشبيه:** زي "ضابط الجمارك". بياخد الشنطة (Input)، ويبص فيها، ويقولك "تمام" (True) أو "ممنوع" (False).
    

**مثال:**

Java

```
// اختبار: هل الرقم موجب؟
Predicate<Integer> isPositive = num -> num > 0;

System.out.println(isPositive.test(10)); // true
System.out.println(isPositive.test(-5)); // false
```

---

### 2. الـ Consumer (الثقب الأسود / الأكّيِل) 🕳️

**س2: إمتى أستخدم `Consumer<T>`؟** **ج:** لما تكون عايز **تستهلك** البيانات (تطبعها، تخزنها في داتابيز) ومش مستني منها رد.

- **الدالة:** `void accept(T t)`
    
- **التشبيه:** زي "سلة المهملات" أو "الطابعة". بتبعتلها الورقة، هي بتاخدها وتتصرف فيها، بس مش بترجعلك ورقة تانية.
    

**مثال:**

Java

```
// دالة بتطبع الاسم وتعمله ترحيب
Consumer<String> greeter = name -> System.out.println("Welcome " + name);

greeter.accept("Mohamed"); // Output: Welcome Mohamed
// لاحظ: مفيش return value
```

---

### 3. الـ Supplier (المصنع / الساحر) 🎩

**س3: إمتى أستخدم `Supplier<T>`؟** **ج:** لما تكون عايز **تنتج** بيانات من الهواء (من غير ما تديها أي input).

- **الدالة:** `T get()`
    
- **التشبيه:** زي "قبعة الساحر". مابتحطش فيها حاجة، بس بتمد إيدك تطلع أرنب (Output).
    

**مثال:**

Java

```
// دالة بترجع تاريخ ووقت اللحظة دي
Supplier<Date> dateSupplier = () -> new Date();

System.out.println(dateSupplier.get()); // بتطبع الوقت الحالي
```

